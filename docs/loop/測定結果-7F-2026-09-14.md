# §7F の測定結果（2026-09-14 17:38 JST）

実行：`nodetime2.py`（task_next.md §1-1 の a〜e をそのまま実装）
条件：1通目のみ／カルテなし／設定変更なし／クライアント側タイムアウト360秒

---

## 0. 先に：§7F の問いは未決です

> 300秒は「返らない」のか、「301秒で返るのに、こちらが300秒で諦めている」のか。

**12回とも120秒未満で、跳ね（120秒超）を捕まえられませんでした。**
したがって §2 の判定表の上3枝には入りません。**4つめの枝です。**

| 観測 | 判定 | 次 |
|---|---|---|
| 12回とも120秒未満 | この時刻には**300秒級の**跳ねが出ない | 時刻を変えてもう1回だけ |

**ただし、この12回で別のことが分かりました。そちらのほうが重要です。**

---

## 1. 結果

```
n=12  p50=21.3  min=14.6  max=60.6  over24=4  over24pct=33
実測: 20.9 / 14.9 / 21.9 / 17.9 / 21.6 / 19.5 / 40.2 / 17.6 / 14.6 / 60.6 / 24.3 / 28.5
```

### 1回ごとの内訳（秒）

| 回 | 実測 | LLM | **GAS 2本の合計** | first_token |
|---|---|---|---|---|
| 1 | 20.9 | 5.83 | 2.13 | 19.6 |
| 2 | 14.9 | 5.24 | 2.26 | 13.7 |
| 3 | 21.9 | 5.37 | 3.19 | 20.2 |
| 4 | 17.9 | 6.29 | 3.50 | 16.6 |
| 5 | 21.6 | 5.32 | 3.84 | 20.4 |
| 6 | 19.5 | 4.21 | 5.22 | 19.4 |
| **7** | **40.2** | 4.77 | **25.15** | 39.3 |
| 8 | 17.6 | 5.11 | 3.20 | 15.9 |
| 9 | 14.6 | 4.79 | 3.02 | 13.4 |
| **10** | **60.6** | 6.90 | **44.42** | 59.9 |
| **11** | **24.3** | 4.75 | **10.01** | 23.4 |
| **12** | **28.5** | 7.07 | **11.52** | 27.1 |

GAS 2本＝`HTTP リクエスト（カルテ照会）` ＋ `HTTP リクエスト（相談記録登録：初回受付）`

### ノード別（12回）

| ノード | p50 | max | min |
|---|---|---|---|
| **LLM** | **5.28** | **7.07** | **4.21** |
| HTTP（カルテ照会） | 3.28 | **17.25** | 1.69 |
| HTTP（相談記録登録：初回受付） | 3.79 | **27.17** | 1.33 |
| パラメータ抽出 | 2.51 | 3.46 | 2.27 |

**全12回とも `llm_started = yes` ／ `終了のしかた = 正常` ／ `last_event = workflow_finished`。**

---

## 2. 何が分かったか

### 跳ねの原因は GAS へのHTTP 2本です

```
24秒以内の8回  … GAS 2本の合計 = 2.1 / 2.3 / 3.2 / 3.5 / 3.8 / 5.2 / 3.2 / 3.0
24秒超の4回    … GAS 2本の合計 = 25.1 / 44.4 / 10.0 / 11.5
```

**例外なく対応しています。**

### LLM は主因ではありません

**60.6秒かかった回でも、LLM は 6.90秒です。** 12回すべてで 4.21〜7.07秒に収まっており、
跳ねた回と収まった回で差がありません。

### 前回の私の結論を訂正します

差分（`program-差分-2026-09-14.md`）で、私はこう書きました。

> **Aの正体**：普段7秒のLLM呼び出しが、時々まったく返らなくなる
> **したがって、次にやることは §3 の7（LLMのタイムアウトと再試行）です**

**今回の12回では、LLMの停止は1回も起きていません。**
前回の6回目（300秒打ち切り・LLMの完了イベント来ず）は**別の、より稀な事象**です。
それを1回観測しただけで「跳ねの主因」と断定したのが誤りでした。

**日常的な over24（今回33%）を作っているのは、GASへのHTTPです。**

### program.md §7 の仮説3について

> 3. 1通目だけが持つカルテ照会の往復が30秒級
> **反証：10回測って1.0〜4.8秒**

**この反証は、たまたま速い10回を引いただけでした。**
今回は同じノードで **17.25秒** を観測しています（12回中）。

**仮説3は復活します。** ただし正確には「カルテ照会が常に遅い」ではなく
**「GASへのHTTPが、時々20〜27秒級になる」**です。

---

## 3. 打ち手（存在を確かめたかどうかを明記します）

前回、存在しない設定を打ち手として書いた反省から、区別して書きます。

### 打ち手A：`初回受付` を「回答」より後ろに移す　**存在を確認済み**

**確認方法**：本番のワークフローで、`コード（追記保存の結果判定）` と
`IF/ELSE 13` が **`回答（追記受付）` より後に実行されている**のを、
ノードの完了順で観測しました（2026-09-12）。
**Difyは回答より後ろでノードを走らせられます。**

初回受付の登録は**相談者への返事に必要ありません。**
記録するだけなので、返事を出してから走らせれば、**critical path から消えます。**

今回のデータでは、これだけで **27.17秒が返事の待ち時間から外れます。**

> ⚠️ ただし、いま初回受付の後ろに繋がっている
> `変数代入（初回受付ログ済みフラグ）` と `IF/ELSE 14（保存の失敗チェック）` の
> 配線を一緒に動かす必要があります。**構造変更です。**

### 打ち手B：`カルテ照会` のタイムアウトと再試行を詰める　**存在を確認済み（値は未確認）**

**確認方法**：そちらの報告（2026-09-14）に
「`http-request` ノードは `timeout: {max_connect_timeout, max_read_timeout, max_write_timeout}` と
`retry_config: {max_retries, retry_enabled, retry_interval}` を持つ」とあります。

**現在値は私には読めません。** program.md §3 では `max_read_timeout` = 10秒 とあります。
しかし**17.25秒を観測している**ので、

- 再試行が上乗せされている（10秒＋間隔＋10秒…）か
- このノードの値が10秒ではない

のどちらかです。**まず現在値を読んでください。**

カルテ照会は LLM より前に必要なので、**critical path から外せません。**
**5秒で切って再試行なし**にすれば、遅いときは「カルテなし」で進みます。
初めての方と同じ動きになるだけで、**相談は止まりません**（2026-09-14 に確認済み）。

### 打ち手C：`§9②` の2段返し　**人の作業**

`first_token` が 13.4〜59.9秒で、**実測との差は 0.7〜1.7秒**でした。
つまり**本文は最後の1〜2秒で一気に流れており、それまで相談者には何も見えません。**

AとBで詰めても、**上流ノードだけで8〜15秒**を使います。24秒の窓は依然として際どいままです。

---

## 4. `loop_log.md` への追記行

```markdown
| 09-14 17:38 | （測定のみ）§7F タイムアウト360秒・n=12 | 12 | 21.3 | 60.6 | 33 | なし | — | 120秒超の跳ねは捕まらず（§2の4枝め）。ただし over24 の4回は**GASへのHTTP2本が10.0〜44.4秒**で、LLMは全12回 4.21〜7.07秒。跳ねの主因はLLMではなくGAS。first_token 13.4〜59.9秒（実測との差0.7〜1.7秒） |
```

### 「次にやること」の書き換え案

```markdown
## 次にやること

§7F の300秒問題は未決（今回は捕まらず）。だが over24pct の主因が
**GASへのHTTP2本**だと分かったので、そちらを先に潰す。

1. `カルテ照会` と `初回受付` の `timeout` / `retry_config` の**現在値を読む**（変更しない）
2. 読んだ値を loop_log に記録する
3. そのうえで打ち手A（初回受付を回答より後ろへ）と
   打ち手B（カルテ照会のタイムアウト短縮）を、人に諮る

**§3 の 1〜4（モデル・thinking・memory・プロンプト）には引き続き打ち手が無い。**
LLMは全12回 4.21〜7.07秒で安定しており、跳ねに寄与していない。
```

---

## 5. 台帳に増えた行数

**相談記録に12行**（呼び名「そうた」・30代・男性・徳島県・仕事）。
消すのは人です。

---

## 6. 測定方法について

スクリプト：`tools/dify-test/nodetime2.py`
（`eshanten18111/tetote-liff` の `claude/dify-testing-continued-j1xrb4`）

task_next.md §1-1 の a〜e をそのまま実装しています。
`DIFY_API_KEY` を設定すれば、どの環境からでも同じ測定ができます。

```bash
export DIFY_API_KEY='app-xxxxxxxx'
python3 nodetime2.py
```
