# loop_log.md — 実験の記録（追記のみ。上書きしない）

program.md §6 の形式で、1実験1行。

| 日時 | 変えたもの | n | p50 | max | over24pct | 追記定型文 | 判定 | 備考 |
|---|---|---|---|---|---|---|---|---|
| 09-14 15:37 | （基準）pro / thinking 1024 / カルテなし | 3 | 63.6 | 65.6 | 67 | なし | — | simLoop 導入前。手で3回 |
| 09-14 15:47 | （基準）pro / thinking 1024 / カルテあり | 3 | 24.7 | 120+ | 33 | なし | — | 1回目は504（ゲートウェイ切り） |
| 09-14 16:09 | model → gemini-3.6-flash | 2 | 36.8 | 52.7 | 50 | なし | **revert** | 2通目は3倍速。1通目は17%減にとどまる。§5未通過のため本番から戻した |
| 09-14 17:07 | （測定のみ）pro / thinking 1024 / カルテなし / **API直接** | 6 | 20.6 | 300+ | 17 | なし | — | ノード内訳あり。LLMが1回停止（完了イベント来ず）。他5回はLLM 4.3〜8.1秒 |
| 09-14 17:07 | （測定のみ）同時刻に `simRun` | 8 | 21.6 | 56.1 | 38 | なし | — | 上と同時刻。p50はほぼ同じで、違いは跳ねだけ → 経路のせいではない |
| 09-14 18:0x | （調査のみ）LLMノードに timeout / retry があるか | — | — | — | — | — | **打ち手なし** | 項目に timeout が無く retry_config は null。Difyの再試行はエラー時のみ。機能要求 #31187 は closed as not planned |
| 09-14 17:38 | （測定のみ）§7F タイムアウト360秒・n=12 | 12 | 21.3 | 60.6 | 33 | なし | — | 120秒超の跳ねは捕まらず（§2の4枝め）。ただし over24 の4回は**GASへのHTTP2本が10.0〜44.4秒**で、LLMは全12回 4.21〜7.07秒。跳ねの主因はLLMではなくGAS。first_token 13.4〜59.9秒（実測との差0.7〜1.7秒） |
| 09-14 18:2x | （調査のみ）HTTPノード14本の `timeout`／`retry_config` の**現在値を読む** | — | — | — | — | — | **打ち手A・Bを人へ** | `カルテ照会`(1789000000002) と `初回受付`(1785254830456) はいずれも **connect5/read10/write5・再試行1回・間隔2000ms → 1回15秒・最悪32秒**。観測（17.25／27.17秒）はこの内側で説明がつく。前例：`初回受付の送り直し`(1786800000003) は **3/5/3・再試行なし**。**変更はしていない** |
| 09-14 18:06 | （測定のみ）階級と打ち手見積り | 12 | 25.4 | 102.5 | **58** | なし | **見積り出せず** | §2の5枝め：実行7で 実測102.5 < ノード合計108.3。**ただし止める条件を書いた側の誤り**（対象2ノードは直列で式は使えた）。カルテ照会 max 58.29／初回受付 max 72.76 で「最悪32秒」を超過 → **その式を取り消した**（§7G-3） |
| 09-14 18:3x | （調査のみ）並列箇所の特定と「最悪32秒」の検証 | — | — | — | — | — | **A優位の可能性** | 同一出力からの多重辺は3箇所のみ（LLM後／追記本文抽出後／初回受付後）。**カルテ照会・初回受付は入る辺が1本＝直列**。read タイムアウトは「間隔の上限」で全体の上限ではないため、**遅いが生きている応答には発火しない** → 打ち手Bの効き（最悪8秒）も取り消し |
| 09-14 18:42 | （測定のみ）2通目以降・2会話×8往復 | 16 | 21.8 | 36.2 | 31 | なし | — | **6通目以降（GASなし）でも p50 22.5・over24 33%。打ち手A+Bでは目標に届かない**。LLMが往復とともに伸びる（1通目7.86 → 2〜5通目9.04 → 6通目以降13.51、max 22.72）。§7の「1〜4に打ち手なし」は1通目のデータに基づく分類だった。項目3（memory.window）は2通目以降で再評価の対象（未測定） |
| 09-14 18:58 | （測定のみ）トークンとLLM時間の関係・24往復 | 24 | 22.2 | 105.6 | 29 | なし | — | **入力は5%しか増えない（29,757→31,243）／出力は2.7倍（538→1,618）／LLMは2倍**。判定表の2枝め＝履歴ではない。thinking_budget が候補。**遅いGAS 6件すべて status=200・node_retry=0・location なし → 打ち手Bは弱い。Aを先に**。first_token p90：1通目76.1／2〜5通目34.3／6通目以降21.5。first_tokenと実測の差 p50 1.4秒 |
| 09-14 19:0x | （反映のみ）§1 の主指標を全局面に拡張。§7H・§7H-2 を追加 | — | — | — | — | — | **本線が §9② に移った** | 「1通目の over24pct」→「全局面の over24pct、3局面に分けて記録」。ループの役割は「設定を詰める」から「2段返しの設計値を揃える」に変更 |

| 09-14 21:2x | （調査のみ）Worker のコードを読む | — | — | — | — | — | **前提の訂正＋欠陥3件** | 「24秒」は窓ではなく自分で決めた見切り（本当の壁は waitUntil の約30秒）。**いまの実装はすでに2段返し**。欠陥①1通目が遅いと会話IDが保存されず、拾い直しが【新しい会話】で聞き直す＝AIが二重に答え、記録の行が2つでき、会話変数が消える。②覗きの空振りを「やり直し」と数え、2段目の到着が約3分。③doGetの案内文を素通りさせ記録が黙って落ちる。**3点ともコードを修正・手元テスト32件合格・デプロイは人** |

| 09-14 22:0x | （反映のみ）ループの実機検証を受けて Worker を追補 | — | — | — | — | — | **欠陥4件に** | 走査の4前提（並び順・limitの向き・conversationsの既定・生成中の行）は**すべて実機で正しいと確認**。依頼の `&sort_by=-updated_at` は**デプロイ前なので今入れた**（追加デプロイは発生しない）。「受け取りより前の行は配らない」門も追加（created_at が無ければ門を閉めない）。**そちらの障害報告から④が見つかった**：満杯の400で生きている会話IDを捨てていた → 会話変数の初期化・記録の重複・二重課金。テスト47件合格。**デプロイは未実施** |

| 09-14 23:0x | （反映のみ）ループが見つけた門の穴を修正 | — | — | — | — | — | **回帰テスト追加** | `p.at` は鍵の時計を兼ねており、聞き直しのたびに更新される。それを受け取り時刻として門に渡していたため、**聞き直しに1度失敗すると、できている返事を「昨日の行」と誤判定して捨て、作り直していた**（＝止めたかった事故そのもの）。`recv` を別に持たせて解決。`looksStaleConversation` も実測2文に絞った（会話変数のエラー文で誤判定する恐れ）。テスト58件合格。**デプロイは未実施** |

## 次にやること

**ループ側に、単独で進められる実験はもうありません（§8 の止めどき）。**

`task_next.md` 第4版（トークンとLLM時間の関係）は 18:58 に実施済み。3つ確定した。

1. **打ち手Bは無効**（遅いGAS 6件すべて status=200／node_retry=0／location なし）。
   read タイムアウトは発火していない。`read` を5秒に詰めても変わらない
2. **`memory.window` も効かない**（入力は8往復で+5%のみ。伸びているのは出力で2.7倍）。
   §3 の項目3 は候補から外してよい
3. **2段返しの1段目は Dify を待てない**（first_token は実測の1.4秒前、1通目 p90 76.1秒）。
   1段目は Worker が Dify を待たずに送る設計になる

残る Dify 側の候補は **§3 の項目2（thinking_budget）のみ**で、
**人の決定＋§5 の安全確認が要る**（出力が伸びていることが根拠だが、見立ての深さと引き換え）。

**次に動くのは人である**（GAS側の実行時間 → Worker のデプロイ → お待たせ通知の出しどき → 打ち手A）。
ループは、そのあと「前後を比べる測定」で再開する。**それまで新しい実験を始めないこと。**

## 済んだこと

- 仮説A（時間帯依存／LLM停止）は**実在するが稀**。日常の over24 の主因ではない（§7G で格下げ）
- 仮説B・C・D は否定
- **仮説3・4は復活**（§7G）。人間側の反証は Apps Script→GAS を測った**標本の取り違え**だった
- 時間帯を変えて `simRun` を3回回す計画は不要（内訳が取れるようになったため）
- HTTPノードの現在値は**読み終わった**。推測で書かないこと
- **GASを外しても24秒を超える**ことが実測で確定（§7H）。**ループ側の設定だけでは目標に届かない**
