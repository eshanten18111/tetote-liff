# program.md への差分（2026-09-14）

読む相手：**program.md を管理する人**（適用は人が行ってください）
根拠となる実測：この文書の末尾（§実測データ）

---

## なぜ差分が要るか

いまの program.md には、こう書かれています。

> B. パラメータ抽出2つ（gemini-3.6-flash）のどちらかが時々詰まる
> → 節点ごとの所要時間が要る。**Difyのログ画面には内訳が出ず、APIは401で閉じている**
>
> **Aを先に潰すこと。**

**この2つの前提が、どちらも事実と違っていました。**

1. **ノードごとの所要時間は、公開APIで取れます。** 401で閉じているのは管理用API（`/console/api`）です
2. **実際に測ったところ、B・Cは否定され、Aの正体も特定できました**

この状態でループを回すと、エージェントは**測れるはずのものを測らず、効かない設定変更を繰り返します。**

---

## 差分1：§2「測り方」に節を追加

`### n=8 では足りないとき` の**直後**に、次の節を挿入してください。

````markdown
### ノードごとの所要時間を取る（`simRun` より速く、内訳が出る）

公開APIの `/v1/chat-messages` をストリーミングで叩くと、`node_finished` イベントに
各ノードの `elapsed_time` が入ります。**管理用APIの認証は要りません。**
必要なのはアプリのAPIキー（`DIFY_API_KEY`）だけです。

```bash
export DIFY_API_KEY='app-xxxxxxxx'
python3 nodetime.py       # 既定で6回。N を書き換えれば増やせる
```

出るもの：

```
=== 実行3  実測 20.2秒 ／ ノード合計 17.4秒 ／ 差 2.8秒
      8.11秒  LLM
      2.79秒  HTTP リクエスト（カルテ照会）
      2.35秒  パラメータ抽出
```

**「実測」と「ノード合計」の差**が §7 の仮説C（ワークフロー内の固定費）を、
**同じノードの max と min の開き**が仮説B（時々詰まる）を、それぞれ直接測ります。

**この方法には Apps Script の6分上限も、n=8 の縛りも、4分半の打ち切りもありません。**
`simRun` は「相談者から見た所要時間」を測るのに残し、
**原因の切り分けにはこちらを使ってください。**

スクリプト：`tools/dify-test/nodetime.py`
（`eshanten18111/tetote-liff` の `claude/dify-testing-continued-j1xrb4`）
````

---

## 差分2：§3 の表に「7」を追加

§3の表の末尾に、次の行を足してください。

```markdown
| 7 | `llm` のタイムアウトと再試行 | 未設定（止まったまま待つ） | 20〜30秒で切って1回やり直す | 切るのが早すぎると正常な回まで捨てる |
```

そして表の下に、次を追記してください。

````markdown
### 7について（いまの主因はここです）

2026年9月14日の実測で、**LLMノードが時々まったく返らなくなる**ことが分かりました。
普段は4〜8秒で終わるのに、止まると300秒でも返りません（§7の表を参照）。

**1〜6をどれだけ触っても、この停止は直りません。**
モデルを速くしても、止まるときは止まります
（`gemini-3.6-flash` で1通目が52.7秒にとどまったのも、同じ現象と考えられます）。

20秒で切って1回やり直せば、止まった回も「20秒＋7秒」で返せます。
**これが §3 のなかで唯一、跳ねに効く項目です。**
````

---

## 差分3：§7 を差し替え

§7 の**表以降を、まるごと**次に差し替えてください。
表そのもの（外れた仮説1〜4）は残します。

````markdown
**残っていた未検証の仮説 — 2026年9月14日に決着しました**

| # | 仮説 | 判定 | 根拠 |
|---|---|---|---|
| A | Gemini 側の混み具合（時間帯依存） | **これが主因。ただし「遅くなる」ではなく「止まる」** | 下記 |
| B | パラメータ抽出2つのどちらかが時々詰まる | **否定** | 6回とも 2.19〜2.84秒。開きは0.65秒 |
| C | ワークフロー内のどこかに固定費がある | **否定** | 実測とノード合計の差は正常時 2.4〜4.7秒 |
| D | Apps Script 経由（`UrlFetchApp`）が原因 | **否定** | API直接でも同じ跳ねが出る |

**Aの正体**

同じ時刻に2つの経路で測ったところ、**中央値はほぼ同じで、違うのは跳ねだけ**でした。

| 経路 | n | p50 | max | over24pct |
|---|---|---|---|---|
| `simRun`（Apps Script 経由） | 8 | 21.6 | 56.1 | 38 |
| `nodetime.py`（API直接） | 6 | 20.6 | **300超**（打ち切り） | 17 |

跳ねた回の内訳が、API直接の測定で取れています。

```
=== 実行6  実測 300.0秒 ／ ノード合計 9.5秒 ／ 差 290.5秒
      3.84秒  HTTP リクエスト（カルテ照会）
      2.84秒  パラメータ抽出
      …（LLM は一覧に出ていない＝完了イベントが来ていない）
```

**LLMより手前のノードは全部あわせて 9.5秒で正常。LLMだけが止まっています。**

つまり壊れ方は「全体が均等に遅くなる」ではなく、
**普段7秒のLLM呼び出しが、時々まったく返らなくなる**です。
p50が20秒台なのに max が跳ねるのは、これで説明がつきます。

**したがって、次にやることは §3 の7（LLMのタイムアウトと再試行）です。**
1〜6は跳ねに効きません。

**もう一度「原因だ」と言ってはいけないもの**：仮説1〜4、B、C、D。
````

---

## 差分4：§8「ループの止めどき」に1行追加

箇条書きの**3つめの直後**に、次を挿入してください。

```markdown
・LLMの停止が跳ねの主因だと再確認できた（§7のA）→ §3の7を試す。
  それでも over24pct ≤ 20 に届かなければ、**ループを止めて §9② を人に渡す**。
  LLMが止まる以上、24秒の窓を伸ばしても解決しない。返事を2段に分けるほうが確実
```

---

## 差分5：`loop_log.md` に2行追記

```markdown
| 09-14 17:07 | （測定のみ）pro / thinking 1024 / カルテなし / API直接 | 6 | 20.6 | 300+ | 17 | なし | — | ノード内訳あり。LLMが1回停止（完了イベント来ず）。他5回はLLM 4.3〜8.1秒 |
| 09-14 17:07 | （測定のみ）同時刻に simRun | 8 | 21.6 | 56.1 | 38 | なし | — | 上と同時刻。p50はほぼ同じで、違いは跳ねだけ |
```

---

## 実測データ（この差分の根拠）

測定日時：2026年9月14日 17:07 JST（08:07 UTC）
方法：`/v1/chat-messages` をストリーミングで直接叩き、`node_finished` の `elapsed_time` を集計
条件：1通目のみ／カルテなし（台帳に無いLINEユーザーID）／設定は本番のまま

### 1通目の実測（秒）

```
20.9 / 18.9 / 20.2 / 16.6 / 22.7 / 300.0（打ち切り）
p50 = 20.6   over24 = 1/6 = 17%
```

### ノード別（6回ぶん）

| ノード | p50 | max | min |
|---|---|---|---|
| LLM | 5.90 | 8.11 | 4.31 |
| HTTP（カルテ照会） | 3.09 | 3.84 | 1.80 |
| パラメータ抽出 | 2.39 | 2.84 | 2.19 |
| パラメータ抽出3（有人対応同意） | 1.50 | 2.01 | 1.29 |
| HTTP（相談記録登録：初回受付） | 1.05 | 3.01 | 0.88 |
| 以下すべて | 0.31以下 | 0.62 | 0.05 |

※ LLM の n は5です（6回目は完了イベントが来ていないため集計外）

### 前日の基準値との差について

program.md §6 の基準値は **p50 63.6秒・over24pct 67** ですが、
今回の測定は **p50 20.6秒** でした。**3倍の開きがあります。**

跳ねの頻度が時刻によって変わるためと考えられます。
**基準値を1つの数字で持つより、`p50` と `over24pct` を時刻つきで並べるほうが実態に合います。**

---

## 差分を書いた側からの注記

この差分は、program.md を読む**ループのエージェントに向けた文書**です。
適用するかどうかは人がご判断ください。

差分1で紹介している `nodetime.py` は、**本番の設定を一切変えません。**
読み取りだけで、台帳には1通目の受付行が実行回数ぶん増えます
（今回の測定で6行。呼び名「そうた」・徳島県）。
