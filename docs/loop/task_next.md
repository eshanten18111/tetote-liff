# task_next.md — 第5版：**いま新しい実験はありません**

読む相手：**ループのエージェント**
発行：2026年9月14日 23時台（**第4版を差し替え。第4版は 18:58 に実施済み**）
前提：`program.md` §8（止めどき）／`loop_log.md` の 09-14 18:58 の行

---

## 0. まずこれを読むこと

**第4版（トークンとLLM時間の関係・24往復）は済んでいます。**
**もう一度流さないでください。** 流しても結論は変わらず、相談記録に3行増え、12〜15分かかります。

そして、いま**あなたが単独で進められる実験はありません。**
これは行き詰まりではなく、`program.md §8` に書かれた**止めどきに予定どおり着いた**という意味です。

---

## 1. なぜ実験が無いのか（§3 の6つの現在）

| # | 触ってよいもの | いまの状態 |
|---|---|---|
| 1 | モデル | 測定済み。1通目は17%減にとどまる。**§5未通過なので本番に残せない** |
| 2 | `thinking_budget` | **唯一残っている候補。ただし人の決定＋§5の安全確認が要る** |
| 3 | `memory.window` | **測定で消えた**（入力は8往復で+5%。伸びているのは出力で2.7倍） |
| 4 | プロンプトの章 | 削ってよい章がない（§3 の注記） |
| 5 | HTTPの `timeout` | **打ち手Bは無効と測定で確定**（遅いGASは status=200・再試行0回） |
| 6 | HTTPの `retry_config` | 同上 |
| 7 | LLMの timeout / retry | **設定が存在しない**（確認済み） |

**2だけが残っていて、それには人の決定が要ります。**

---

## 2. やること：待つ。ただし2つだけ、やってよいことがある

### 2-1. 何もしないで待つ（既定）

人が動く順番はこうなっています。

```
① GAS側の実行時間の確認（Apps Script の「実行数」→ 種類=ウェブアプリ → 18:58前後）
② Worker のデプロイ（58件版。用意済み）
③ 「お待たせしています」の出しどきの判断（①と、LINEの「…」の実測が前提）
④ 打ち手A を下書きに入れるかの判断
```

**①〜④のどれも、あなたには手が届きません。** 待つのが正しい。

### 2-2. 求められたら答える（受け身の作業）

人や作業側から**具体的に聞かれたとき**だけ、次をやってよい。

```
・すでに取った測定値の再集計（新しく叩かない）
・こちらが渡した文書の前提の検証（実機で測れる範囲だけ）
・Worker のパッチの再検証（今日2回やったもの。要求があれば）
```

**自分の判断で本番のDifyに新しい生成を投げないこと。** 1回ごとに課金され、台帳に行が増えます。

---

## 3. 人の決定が来たら、何を測るか（先に決めておく）

決定の中身ごとに、次の実験が決まります。**来た時点でこの表を見ること。**

| 人の決定 | あなたがやること |
|---|---|
| **Worker をデプロイした** | 変更前後の比較。**ただし Dify 側の数値（p50・over24pct）は変わらない**（Workerは届け方を直しただけ）。測るのは「2段目が届くまでの時間」で、それは**Dify APIからは見えない**。→ **測定の依頼が来るまで動かない** |
| **打ち手A を入れて公開した** | 第4版と同じ条件（3会話×8往復）で測り直し、**局面別**（1通目／2〜5通目／6通目以降）で前後を比べる。予想は2〜5通目の over24pct が 33%→17% |
| **`thinking_budget` を下げて公開した** | 同じ条件で測り直す。**あわせて §5 の台本6本を流し、返事の中身を人が読める形で並べる。** 速さの数値が良くても、§5未通過なら `hold` と記録する |
| **カルテ照会を1通目から外して公開した** | 同じ条件で測り直す。1通目の p50 が 58.4秒からどこまで落ちるか |
| **何も変えないと決まった** | 記録を締めて止まる |

**どの枝でも、公開は人がやります。** あなたは下書きを変えず、公開を試みないこと（塞がれています）。

---

## 4. やってはいけないこと

```
・第4版（あるいは過去の版）の測定をもう一度流す（済んでいる）
・Dify の下書きを変更する（§3 の1〜6すべて。thinking_budget を含む）
・公開しようとする（[Production Deploy] で塞がれている。迂回路を探さない）
・program.md §4 のもの（歯止め3つ／Code.gs／鍵の値／environment_variables／本番のLINE／既存の行）
・§7 の仮説1・2、B、C、D を「原因だ」と言い直す（仮説3・4 は言ってよい）
・自分の判断で新しい生成を投げる（課金と台帳の行が増える）
```

---

## 5. 記録

**この版では、記録することがありません**（測らないため）。
人の決定が来て §3 の実験を1つ流したときに、`loop_log.md` に1行追記する。

---

## 6. 手元の版の確認（渡されたものが最新かどうか）

文書が古いまま渡ることが一度起きています。**受け取ったら、まずこれを確かめてください。**

```
program.md   … "script.google.com" を検索して 0件 なら最新
                （Apps Script のURLは削除済み。あれば古い版）
loop_log.md  … "18:58" の行があれば最新
task_next.md … 見出しが「第5版」なら最新（この文書）
```

**古ければ、作業側に言ってください。** こちらの渡し方の問題です。
