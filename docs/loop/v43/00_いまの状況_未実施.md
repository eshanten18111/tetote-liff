# 第43版　いまの状況（**まだ流していません**）

> **これは測定の報告ではありません。** 第43版の台本Cは**1往復も流していません**。
> このファイルは、いつ・どう流すかを作業側に伝えるための連絡です。
> 書いた時刻：2026年9月21日（月）00:33 JST

---

## 1. ひとことで

**第43版は、今夜23:47 JST まで流せません。** 0時をまたぐ必要があるためです。
道具は用意して push 済み、起きる予約も入れてあります。**作業側で必要な手当てはありません。**

---

## 2. なぜ今すぐ流せないか

第43版の指示書に、こうあります。

```
今回は、わざと0時をまたいでください。夜23時50分ごろに流し始め、
同意のあとの C-a〜C-c のどれかが0時を過ぎるようにします。
```

指示書を受け取ったのが **9月21日（月）00:30 JST** で、**0時はその30分前に過ぎたあと**でした。
次に0時をまたげるのは今夜（9月21日→22日）です。

いま流すと、C-7（**0時を過ぎてから送った回でも `相談記録登録：追記` が動くか**）という
今回いちばんの見どころが測れません。**それでは第42版の × が直ったか確かめられない**ので、待ちます。

---

## 3. いつ流すか

| | |
|---|---|
| 起きる時刻 | **2026年9月21日（月）23:47 JST** |
| 予約ID | `trig_01R3Mr8RHRgnMoyGzfzVyNEW`（1回きり・有効） |
| 流し始め | 23:47〜23:50 ごろ（起きてから時刻を確かめて流します） |
| 終わるころ | 9月22日（火）00:01〜00:05 ごろ |
| 利用者ID | `t-43-C`（新規） |
| 見込みの往復 | 最大20往復（本体16＋足し4）・**約72円**（指示書の上限 21往復・約76円 の内側） |

起きたあとは、測定 → 判定 → 送り状を書く → commit・push → ファイルをお渡しする、まで
こちらで通しでやります。**作業側からの合図は要りません。**

---

## 4. 用意したもの

**`tools/dify-test/v43_C.py`**（commit・push 済み）

0時またぎを取りこぼさないよう、こういう作りにしています。

- 本体は最大16往復。**1往復目で `版 2026-09-20i` と `error_message` が空であることを確かめ、
  違えばその場で止めて知らせます**（429 も同じ）
- 同意のあと C-a／C-b／C-c を送ります
- **そのどれもが0時より前だったら、0時を20秒過ぎるまで待って
  C-d「眠れないまま朝になりそうです。」を送ります**（指示書の §1 の但し書きのとおり）
  - 足しは最大4往復（C-a〜C-d）で、上限の5往復の内側です
- 0時まで1時間以上あるときは、待たずに終わります（取り違えて流さないための歯止め）
- **全往復について、送った時刻（JST）・`IF/ELSE 7` の結果・`相談記録登録：追記` が
  動いたかどうかを記録します**（C-7 の報告に必要なため）

---

## 5. 当日いただける見込みのもの

| # | 見るところ |
|---|---|
| C-1 | 同意の回で `IF/ELSE 6` true、引き継ぎの保存とSlack通知。同意より前には動かない |
| C-10 | 文が抜けて不自然につながっているところがない |
| C-9 | お名前・お呼び名を尋ねていない。お願いはフォーム入力の1つだけ |
| C-2 | 予約フォームのリンクが1本、切れ・記号の混ざりなし |
| C-3 | 行頭の「*」「-」がない、「**」がない |
| C-4 | 電話番号が崩れていない（**出なければ「測れず」**） |
| C-5 | `forced_close` の値。空でない回は何通目か・値・全文 |
| C-6 | C-a の返事に日数の目安・名前・時刻の約束がない |
| **C-7** | **同意のあとの全部で追記が動く。0時を過ぎてから送った回も。各回の送信時刻（JST）と `IF/ELSE 7`** |
| C-8 | このチャットについての「いつでも」がない。外したあとの文が崩れていない |

あわせて、版／日時（JST）／利用者ID／**Slack に飛んだ通知の数と種類（時刻つき）**／費用を書きます。

---

## 6. 第42版で C-7 が × になったときの記録（参考）

直しの効きを見比べるための、前回の実測です。

| 版 | 走った時刻（JST） | 同意の回 | 同意+1 | 同意+2 | 同意+3 |
|---|---|---|---|---|---|
| 第39版（版e） | 21:44〜21:49 | 10通目 | IF7 true・追記 ○ | IF7 true・追記 ○ | IF7 true・追記 ○ |
| 第41版（版g） | 22:38〜22:45 | 16通目 | IF7 true・追記 ○ | IF7 true・追記 ○ | IF7 true・追記 ○ |
| **第42版（版h）** | **23:54〜00:00** | 13通目 | IF7 true・追記 ○ | IF7 true・追記 ○ | **IF7 false・追記 ×** |

第42版で落ちたのは **16通目（00:00:14 JST 送信）** の1通だけでした。
そのとき返事は「そのお気持ちも、担当の者にしっかり伝わっていますのでご安心ください。」と
言っていましたが、**台帳には書かれていません**でした。

---

## 7. いま入っている予約は2件です

| 予約ID | いつ | 中身 |
|---|---|---|
| `trig_01R3Mr8RHRgnMoyGzfzVyNEW` | **9月21日（月）23:47 JST** | 第43版 台本C（0時またぎ）・最大20往復・約72円 |
| `trig_01F3YW8vW9gn9ZiRxczVrBFE` | 9月24日（木）10:00 JST | 別紙 T4（`t-34-T4`・2往復・約7円） |

**T4 の予約文に古いところがあります。**
作った時点の版のまま「`版 2026-09-19z3` のままか」と書いてあります（いまは `2026-09-20h`）。
予約文には「変わっていたらその版を報告に書く」ともあるので動作に支障はありませんが、
書き直しておくこともできます。**ご希望があればお知らせください。**

---

## 8. こちらで決めたことと、変えられること

- **待つと決めたのは私の判断です。** 0時またぎを諦めれば今すぐ流せますが、
  そうすると C-7 が測れません
- **「今夜まで待たず先に流してほしい」ということでしたら、そうします。**
  その場合、C-7 は「測れず」になります
- 台本C以外を先に流す、といった組み替えも可能です

---

## 9. 直近の状態

- ブランチ：`claude/dify-testing-continued-j1xrb4`
- 走っている測定：**なし**
- 未 commit の変更：**なし**
- 直前の完了：第42版（版 2026-09-20h／`t-43-C` ではなく `t-42-C`／16往復／× は C-7 の1つ）
