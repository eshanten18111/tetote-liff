# §5 安全確認（台本6本）— 送り状

実施：2026-09-14 17:44〜18:09
実施者：ループのエージェント
指示：`task_next.md` 第6版

---

## 0. これは判定ではありません

**○×を付けるのは人です。私は付けていません。**
返事は1文字も要約していません。全文がそのまま入っています。

「気づいたこと」は §3 に、**判定と混ぜずに**書きました。
中身のことは書いていません。**材料が材料として使えるかどうか**の話だけです。

---

## 1. 読む順番（ファイル一覧）

| # | ファイル | 中身 |
|---|---|---|
| 1 | `safety6_出力_175621.md` | **台本1・3・4・5** |
| 2 | `safety6_出力_174411.md` | **台本2** |
| 3 | `safety6_出力_180240.md` | **台本6**（カルテ持ちの1通目） |
| 4 | `safety6_補足_取り直し.md` | **↑の中で応答が返らなかった5往復**の返事（§2） |
| 5 | `safety6_出力_台本5流し直し_180853.md` | **台本5はこちらを読んでください**（§3-1） |
| 6 | `safety6_出力_180601.md` | 台本5の2回目。**使えません**（§3-1）。記録として置いてあります |

**台本5については、5番のファイルだけを見てください。**
1番と6番に入っている台本5は、会話が途中で分かれていて材料になりません（§3-1）。

---

## 2. 実行の条件（そのまま書きます）

```
スクリプト … いただいた safety6.py をそのまま使用。書き換えていません
              md5 = 4b5668ff2698c1783ecfba2940f160a3
台本の文   … 1文字も変えていません（台本4の電話番号・住所も台本のまま）
設定       … Dify の下書きは1つも変えていません
利用者ID   … すべて t-s5- で始まるもの
本番のLINE … 触れていません
鍵         … この環境はプロキシが鍵を入れる仕組みのため DIFY_API_KEY=dummy を渡しました。
              プロキシが Authorization を上書きするので実際の鍵で通っています。
              **鍵の値は読んでいません。出力にも入っていません**（検査済み）
```

### 台本6の前提（第6版 §1-1 のとおりにやりました）

```
1. 台本2を先に流した（17:44:11〜17:46）。利用者ID = t-s5-174411-2
2. 16分あけた
3. KARTE_USER=t-s5-174411-2 で台本6を流した（18:02:40）
```

**KARTE_USER 未指定の警告は出ていません**（出力に0件）。
台本6は新しい会話として流れています（`また相談させてください` の1往復）。

---

## 3. 気づいたこと（判定ではありません。材料の話です）

### 3-1. 台本5は、この環境では2回とも会話が分かれました

**台本5の1通目（入口フォーム）が、2回とも30秒ちょうどで切れました。**
会話IDが取れないので、2通目以降が**別の会話**として流れています。

```
1回目 t-s5-175621-5  会話 adec2f7c : 呼び名：ゆう／年代：10代…        （1往復）
                      会話 658ba838 : 今日の天気／小説／野球            （3往復）
2回目 t-s5-180601-5   同じ形で分かれた
```

**この形だと「10代であることを踏まえた言い方になっているか」を見られません。**
年代がその会話に入っていないからです。

そこで3回目は、**台本の文は `safety6.py` から読み込んだまま**（書き写していません）、
切れた1通目の会話IDを `GET /conversations` から拾って、2通目以降をその続きに送りました。

```
3回目 t-s5-180740-5   会話 580d6885 : 4往復すべて同じ会話
```

**`safety6_出力_台本5流し直し_180853.md` が、それです。台本5はこれを読んでください。**

### 3-2. 5往復で、POSTの応答が返りませんでした（Difyの失敗ではありません）

```
台本2 5通目 / 台本3 2通目 / 台本3 4通目 / 台本4 2通目 / 台本5 1通目
```

**5件とも、ちょうど30.1〜30.3秒で `upstream request failed`。**
**ループを動かしているこの実行環境のプロキシが、30秒で接続を切っています。**
**作業側の手元では起きません。**

今日の測定で「このエラーが返っても Dify 側では生成が完走している」ことを
確認済みです（`program.md` §9）。送り直すと二重課金と台帳の行が増えるので、
**送り直さずに** `GET /v1/messages` から、そのときの返事をそのまま取り出しました。

`safety6_補足_取り直し.md` に5件そろっています。**5件とも取得できました。**

> **こちらの手違いを1つ書いておきます。** 最初この取り直しをしたとき、
> どの往復が失敗したかを**私が数えて書いた**ため、**別の往復の返事を拾っていました。**
> 出力ファイルから機械的に拾い直したのが、いま入っている `safety6_補足_取り直し.md` です
> （`recover2.py`）。**間違ったほうのファイルは残していません。**

### 3-3. この環境では、1通目が高い確率で切れます

台本5の1通目は2回とも切れました。台本1・2・3・4の1通目は通っています。
**30秒を境にしているので、1通目が遅い台本ほど切れます。**
作業側の手元で流し直せば、この細工なしで素直に取れます。

---

## 4. 台帳に増えた行（第6版 §5）

**予想は6行でしたが、実際は12行です。消すのは人です。**

| 利用者ID | 会話 | 何の行か |
|---|---|---|
| `t-s5-174411-2` | 2 | 台本2 と 台本6 |
| `t-s5-175621-1` | 1 | 台本1 |
| `t-s5-175621-3` | 1 | 台本3 |
| `t-s5-175621-4` | 1 | 台本4 |
| `t-s5-175621-5` | 2 | 台本5（1回目・分かれた） |
| `t-s5-180601-5` | 2 | 台本5（2回目・分かれた） |
| `t-s5-180740-5` | 1 | 台本5（流し直し・連続） |
| `t-s5-authcheck` | 1 | 鍵の通り方の確認 |
| `t-s5-authcheck2` | 1 | 鍵の通り方の確認 |
| **合計** | **12** | |

増えた内訳（6行 → 12行）：

```
+2 … 台本5が会話ごと分かれたため（1回目・2回目とも2会話になった）
+2 … 台本5を2回流し直したため
+2 … 流す前に「鍵がプロキシ経由で通るか」を確かめた2回
```

**+2（鍵の確認）は、私がやらなくてもよかったぶんです。**
`/v1/parameters` のような生成を伴わない経路で確かめれば、行は増えませんでした。

---

## 5. `loop_log.md` への追記行（第6版 §4）

```markdown
| 09-14 18:09 | （安全確認のみ）§5 台本6本 | 6 | — | — | — | なし | **人の判定待ち** | 出力は docs/loop/safety6/（台本5は「流し直し」のファイルを見ること）。台本6は KARTE_USER=t-s5-174411-2 で流した（警告0件）。この環境のプロキシが30秒でPOSTを切るため5往復が返らず、送り直さず GET /messages から取得（5/5成功）。台本5は1通目が2回とも切れて会話が分かれたため、会話IDを拾って続きに送る形で3回目を実施。**相談記録に12行**（予想6行。内訳は送り状 §4） |
```

---

## 6. 次にやること

**ありません。人が読んで○×を付ける番です。**

読んだ結果が「台本◯が危ない」であれば、そこから先（プロンプトの直し）は
`program.md` §3 の項目4にあたり、**人の決定が要ります。** 私は触りません。
