# 「回答」より後ろに、何秒あるのか（`task_next.md` 第10版）

実施：2026-09-15 14:39〜14:56（JST）
利用者ID：`t-v10a-053912-1` 〜 `t-v10a-054355-6` ／ `t-v10b-054701`

---

## 0. 先に：外に出た副作用があります

**私のテストで、Slack に「初回受付の保存失敗」通知が 2回 飛んでいます。**

```
測定A の 回3 と 回4 で、初回受付の保存が失敗し、
  HTTP（相談記録登録：初回受付の送り直し）
  → コード（初回受付の送り直しの結果判定とSlack本文）
  → HTTP（初回受付の保存失敗Slack通知）
が「回答」より後ろで走りました。
```

**相談者からの通知ではありません。** 同じ時間帯に Slack で保存失敗を見かけたら、
**14:40〜14:44（JST）の2件はこのテストのぶん**です。

（前提の確認：`worker/tetote-line-dify.js:751` が `response_mode: 'blocking'` であることは、
リポジトリのコードで確認しました。）

---

## 1. 測定A — 表（第10版 §1）

| 回 | t_last−t0 | t_end−t0 | **差(t_end−t_last)** | 回答ノード終了−t0 |
|---|---|---|---|---|
| 1 | 38.2 | 40.3 | 2.1 | 38.3 |
| 2 | 29.7 | 30.8 | 1.1 | 29.7 |
| 3 | 94.8 | 98.1 | 3.3 | 94.8 |
| 4 | 78.4 | 89.4 | **11.1** | 78.5 |
| 5 | 23.6 | 24.9 | 1.3 | 23.7 |
| 6 | **166.7** | **167.6** | 0.9 | 166.8 |
| **p50** | 58.3 | 64.9 | **1.7** | |
| **max** | 166.7 | 167.6 | **11.1** | |

**`回答` ノードの終了は、毎回 `t_last` の 0.1秒後**でした（表の右端と左端がほぼ同じ）。
**ずれはありません。**

### 差の中で動いていたノード

**ふつうの回（1・2・5・6）は、毎回まったく同じ5つ**でした。

```
返信整形（サマリー除去）          0.07〜0.47秒
回答                              0.0001秒
IF/ELSE 6（引き継ぎサマリー有無）  0.0002秒
パラメータ抽出3（有人対応同意）    1.16〜1.24秒
IF/ELSE 4（有人対応同意判定）      0.0001秒
```

**第10版 §1 の予想（「ふつうの往復では `パラメータ抽出3` だけが後ろに残る」）は、
おおむね当たっていました。** ただし **`返信整形（サマリー除去）` も `t_last` より後**に
終わっています（0.07〜0.47秒）。予想には入っていなかった1つです。

### 差が大きかった2回（回3・回4）は、別のものが後ろにいました

```
回3 の差 3.3秒 の中身
   （上の5つ）
   HTTP（相談記録登録：初回受付の送り直し）        10.398秒
   コード（初回受付の送り直しの結果判定とSlack本文）  0.081秒
   HTTP（初回受付の保存失敗Slack通知）              0.080秒

回4 の差 11.1秒 の中身
   （上の5つ）
   HTTP（相談記録登録：初回受付の送り直し）        19.492秒
   コード（初回受付の送り直しの結果判定とSlack本文）  0.082秒
   HTTP（初回受付の保存失敗Slack通知）              0.076秒
```

**6回のうち2回で、初回受付の保存が失敗し、送り直しの経路が走りました。**

```
確かめたこと   … 送り直しのHTTPが「回答」より後ろで 10.4秒／19.5秒 動いた。
                 Slack への保存失敗通知もそこで走った
確かめていないこと … なぜ最初の保存が失敗したのか。台帳に行が何行できたのか
                 （GASの画面にもスプレッドシートにも手が届きません）
```

> **回3 で、送り直しが10.4秒かかっているのに、差は3.3秒しかありません。**
> 足し算が合いません。**並列に走っている区間があるはずですが、そこは確かめていません。**
> 回4 は 19.5秒に対して差 11.1秒で、こちらも合いません。
> **差の列は「最後の message から message_end まで」で、その中で何本が同時に動いたかは、
> この測り方では分けられません。**

---

## 2. 測定B — blocking と streaming（第10版 §2）

**6通目以降（GASを1本も通らない局面）で、交互に投げました。**

| 通目 | 投げ方 | 結果 |
|---|---|---|
| 6 | blocking | 20.3秒 |
| 7 | streaming | t_last 20.9 ／ t_end 22.1 |
| 8 | blocking | 19.7秒 |
| 9 | streaming | t_last 20.8 ／ **t_end 98.2** |
| 10 | blocking | **30.25秒で `upstream request failed`**（この環境の30秒切断） |
| 11 | streaming | t_last 2.6 ／ **t_end 32.7** |

```
blocking の所要        20.3 / 19.7          （有効なのは2回。10通目は測定になっていません）
streaming の t_last-t0 20.9 / 20.8 / 2.6     p50 20.8
streaming の t_end-t0  22.1 / 98.2 / 32.7    p50 32.7
```

### 判定：**どちらとも言えません**（第10版 §2 の3つめ）

```
blocking 20.0（p50） は t_last 20.8 に近く、t_end 32.7 からは離れています
  → 「blocking は回答の時点で返っている」ように見えます

しかし 10通目の blocking は 30秒を超えて切られました
  → 回答だけで返っているなら、6通目以降で30秒を超えるはずがありません
    （7・9通目の t_last はどちらも20.8秒）
  → 「blocking は回答より後ろも待っている」ようにも見えます

**この2つが食い違っています。断定しません。**
```

**有効な blocking は2回だけです**（3回目はこの環境の制約で測定になりませんでした）。
第10版 §4 が「B は各4回以上投げるな」と定めているので、**足しませんでした。**

### この測定で足りなかったところ（こちらの設計の落ち度）

**測定Bでは、`t_last` より後に動いたノードを記録していませんでした。**

```
9通目 … t_last 20.8 → t_end 98.2（差 77.4秒）
11通目 … t_last 2.6 → t_end 32.7（差 30.1秒）
```

**6通目以降はGASを1本も通らないはずなのに、77秒の差が出ています。**
**その77秒に何が動いていたかを、私は記録していません。**
測定Aと同じ形で採っていれば分かったはずで、**そこは私の段取りの不足です。**

**この77秒について、原因は何も書きません。**

---

## 3. 数字から言えること（第10版 §3 の範囲で）

```
・差（t_end − t_last）は、**ふつうの1通目では p50 1.7秒・max 3.3秒**
・その中身は毎回同じ5ノードで、実質は
  「返信整形 0.07〜0.47秒」＋「パラメータ抽出3 1.16〜1.24秒」
・**保存の送り直しが走った回だけ、差が 11.1秒まで伸びた**（6回中2回）
・blocking が t_end と一致するか t_last と一致するかは、**どちらとも言えない**
・**6通目以降の streaming で、差が 77.4秒 だった回がある**（中身は未記録）
```

**「だから streaming にすべき」「配線をこう変えるべき」は書きません**（§3）。
Worker の変更もデプロイも設計の決定も人の手です。

---

## 4. 実行の条件

```
回数       … A 6回／B 6通（blocking 3・streaming 3）。§4 の上限を超えていません
送った文   … A は第8版・第9版と同じ入口の文（1文字も変えていません）
             B の下ごしらえ5通と測定6通は、こちらで用意した相談の文です
設定       … Dify の下書きは1つも変えていません
Worker     … 触っていません（読んだだけ）
利用者ID   … t-v10a- ／ t-v10b- で始まるもの
本番のLINE … 触れていません
鍵         … プロキシが入れるため DIFY_API_KEY を渡していません。値は読んでいません
時刻       … container の時計が Dify と一致することを確認済み（第9版 §0）
```

---

## 5. 台帳に増えた行（第10版 §6）

**予想は 6＋3 行、実際は 7行です。**

| 利用者ID | 会話 | |
|---|---|---|
| `t-v10a-053912-1` 〜 `t-v10a-054355-6` | 6 | 測定A |
| `t-v10b-054701` | 1 | 測定B（1会話を11通で使い回しました） |
| **合計** | **7** | |

**それとは別に、Slack に保存失敗通知が2件**飛んでいます（§0）。

**消すのは人です。**

---

## 6. `loop_log.md` への追記行（第10版 §5）

```markdown
| 09-15 14:39 | （測定のみ）回答の後ろに何秒あるか | 6+6 | 58.3 | 166.7 | 100 | なし | — | **差(t_end−t_last) p50 1.7／max 11.1**。ふつうの回の中身は毎回同じ5ノードで、実質は返信整形0.07〜0.47＋パラメータ抽出3 1.16〜1.24。**返信整形も t_last より後**（予想外の1つ）。**6回中2回で初回受付の保存が失敗し、送り直し10.4／19.5秒＋Slack保存失敗通知が回答の後ろで走った**（Slackに2件、私のテスト分）。blocking は t_end とも t_last とも**断定できない**（blocking 20.3/19.7、t_last 20.8、t_end p50 32.7。ただし10通目の blocking は30秒超で切られた＝回答だけで返っていない証拠と食い違う。有効な blocking は2回）。**6通目以降の streaming で差77.4秒の回あり・中身は未記録（こちらの落ち度）**。利用者ID t-v10a-／t-v10b-054701。台帳7行 |
```

---

## 7. 次にやること

**こちらからの提案はありません。** §3 が定めた範囲の数字は上に出しました。

残っている未確認は2つで、**どちらもこちらの落ち度で埋まっていません。**

```
① 6通目以降の差 77.4秒 の中身（測定Bで after ノードを記録していなかった）
② 差が「足し算に合わない」理由（回3：送り直し10.4秒に対して差3.3秒）
```

**①は、測定Aと同じ形で 6通目以降を採れば分かります。** 指示が来れば流します。
**勝手には投げません**（課金と台帳の行が増えるため）。
