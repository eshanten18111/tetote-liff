# 作業側へ ― 58件版の検証は完了です。残っているのは文書3か所だけ

発行：ループのエージェント
日付：2026-09-14
宛先：Worker のパッチを書いた側

---

## 0. 結論（先に3行）

```
① 58件版のパッチは、こちらの検証を全部通りました。修正依頼はありません。そのままデプロイできます
② 前回お渡しした5ファイルと、今回いただいた5ファイルは**バイト単位で同一**でした
③ したがって残っているのは、**文書側の宿題3か所**だけです（§3）
```

---

## 1. いただいたファイルは、前回と同一でした

まず差分を取りました。

| ファイル | 前回との比較 |
|---|---|
| `worker_patch_20260914.diff` | **同一** |
| `worker_retry_test.mjs`（58件） | **同一** |
| `program.md` | **同一** |
| `loop_log.md` | **同一**（＝前回指摘した抜けが、まだ直っていません） |
| `task_next.md` | **同一**（＝第4版のまま。中身はもう済んでいます） |

**コードは完成しています。動いていないのは文書のほうです。**

---

## 2. それでも、もう一度流しました（今回の結果）

同一と分かっていても、当てて流しました。以下は**今回この場で出た数字**です。

### 2-1. 手元テスト58件

```
$ patch -p1 < worker_patch_20260914.diff
patching file worker/tetote-line-dify.js
$ node --check （本体を切り出したもの）   → 構文OK
$ node worker_retry_test.mjs .../worker/tetote-line-dify.js

合格 58 / 不合格 0
```

### 2-2. 実機（**本物の Dify API** に当てたもの）

手元テスト58件は、Dify を偽物に差し替えて**手順だけ**を見るものです。
パッチで `/conversations` の呼び出しが新しく増え、`/messages` の読み方も変わったので、
**そこが本物に当たるか**を別に確かめてあります。KVだけ偽物・HTTPは本物です。

```
  OK  できている返事を返した
  OK  会話IDを復元してKVに入れた          ← 欠陥①の直しが本物に通る
  OK  返事も取れた
  OK  古い行として弾いた（null）           ← 門が効いている
  OK  55秒ずれていても配った                ← 60秒の余裕が効いている
  OK  いま動いている会話を返した            ← sort_by=-updated_at
```

実際に叩かれたURLも見ています。意図どおりです。

```
/conversations?user=…&limit=1&sort_by=-updated_at
/messages?conversation_id=…&user=…&limit=3
```

### 2-3. 前回こちらが見つけた穴（`p.at`）

**塞がっています。** 作業側のテスト O〜R だけでなく、
**こちらが書いた再現テスト**でも確認しました。

```
シナリオ：1通目が遅く、最初の聞き直しが失敗した回
 配られたもの      : 本当の返事（t=0に書けている）
 AIに聞き直した回数: 0
 判定: OK できている返事を配った
```

`recv` が途中で落ちないかも見ました。pending を書いているのは3か所で、
**あとの2か所は `p` をそのまま書き戻すので保たれます。**

```
受け取り時       { text, at: now, recv: now, tries: 0 }
覗いて空振り時    JSON.stringify(p)   → recv 保持
聞き直す直前      JSON.stringify(p)   → recv 保持（at だけ更新）
```

`p.recv || p.at` にしていただいた点について。**そこはこちらが書き落としていました。**
デプロイの瞬間にKVで待っている方が取りこぼされないのは、その一行のおかげです。

---

## 3. 残っている宿題 ― 文書3か所（ここが今回お渡ししたいもの）

### 3-1. `loop_log.md` に 18:58 の行がありません

いまの最後は `09-14 23:0x` ですが、**`09-14 18:58` の測定行が抜けています。**
`task_next.md` 第4版で指示された測定の結果で、済んでいるのに記録されていません。

**そのまま貼れる形にしてあります。**

```markdown
| 09-14 18:58 | （測定のみ）トークンとLLM時間の関係・24往復 | 24 | 22.2 | 105.6 | 29 | なし | — | **入力は5%しか増えない（29,757→31,243）／出力は2.7倍（538→1,618）／LLMは2倍**。判定表の2枝め＝履歴ではない。thinking_budget が候補。**遅いGAS 6件すべて status=200・node_retry=0・location なし → 打ち手Bは弱い。Aを先に**。first_token p90：1通目76.1／2〜5通目34.3／6通目以降21.5。first_tokenと実測の差 p50 1.4秒 |
```

（`loop_log.md` は「追記のみ。上書きしない」なので、**位置は 18:42 の行の下**が自然です）

### 3-2. `loop_log.md` の「次にやること」が、済んだ指示のままです

いまこう書かれています。

```
**`task_next.md` 第4版に従う。** 3会話×8往復で、LLMが伸びる原因を切り分ける。
```

**第4版は 18:58 に実施済みで、結論も出ています。** 書き換え案です。

```markdown
## 次にやること

**ループ側に、単独で進められる実験はもうありません（§8 の止めどき）。**

`task_next.md` 第4版（トークンとLLM時間の関係）は 18:58 に実施済み。3つ確定した。

1. **打ち手Bは無効**（遅いGAS 6件すべて status=200／node_retry=0／location なし）。
   read タイムアウトは発火していない。`read` を5秒に詰めても変わらない
2. **`memory.window` も効かない**（入力は8往復で+5%のみ。伸びているのは出力で2.7倍）。
   §3 の項目3 は候補から外してよい
3. **2段返しの1段目は Dify を待てない**（first_token は実測の1.4秒前、1通目 p90 76.1秒）。
   1段目は Worker が Dify を待たずに送る設計になる

残る Dify 側の候補は **§3 の項目2（thinking_budget）のみ**で、
**人の決定＋§5 の安全確認が要る**（出力が伸びていることが根拠だが、見立ての深さと引き換え）。

**次に動くのは人である**（GAS側の実行時間 → Worker のデプロイ → お待たせ通知の出しどき → 打ち手A）。
ループは、そのあと「前後を比べる測定」で再開する。
```

### 3-3. `task_next.md` が第4版のままです

**第4版は済んでいます。** いまの状態でループが読むと、**同じ測定をもう一度流します。**
（相談記録に3行増え、12〜15分かかり、結論は変わりません）

第5版を出すか、あるいは**「人の決定が来るまで新しい実験はない」と明記して差し替える**のが要ります。
中身はこちらからは書けません（何を次に測るかは、人の決定の中身で決まるため）。

---

## 4. ループの状態 ― `program.md` §8 の止めどきに着きました

§3 の「触ってよいもの」6つが、いまどうなっているかです。

| # | 触ってよいもの | いまの状態 |
|---|---|---|
| 1 | モデル | 測定済み。1通目は17%減にとどまる。**§5未通過なので本番に残せない** |
| 2 | `thinking_budget` | **唯一残っている候補。ただし人の決定＋§5が要る** |
| 3 | `memory.window` | **測定で消えた**（入力は8往復で+5%） |
| 4 | プロンプトの章 | 削ってよい章がない（§3 の注記） |
| 5 | HTTPの `timeout` | **打ち手Bは無効と測定で確定** |
| 6 | HTTPの `retry_config` | 同上 |
| 7 | LLMの timeout/retry | **設定が存在しない**（確認済み） |

そして `task_next.md` §2 に「**今回は測るだけ・下書きは変えない**」と明記されています。

```
→ ループが単独で進められる実験は、もうありません
→ 測って決着がつく問いも、こちらの手が届く範囲には残っていません
```

**これは行き詰まりではなく、§8 に書かれた止めどきに予定どおり着いた、という意味です。**

---

## 5. 人の手が要るもの（こちらでは測れません）

| 問い | なぜ測れないか | 優先 |
|---|---|---|
| **GAS側が何秒かかっているか** | Apps Script の「実行数」画面。UIの絞り込みに手が届きません | **いちばん先** |
| Worker のデプロイ | Cloudflare の公開作業 | 用意できています |
| LINEの「…」が実際に何秒見えるか | LINEの画面。手が届きません | §5の判断がここに懸かる |
| プッシュの通数と費用 | `manager.line.biz`。お金に関わるので触りません | — |
| 打ち手A／お待たせ通知の出しどき | 人の決定 | — |

**GAS側の実行時間が最優先**です。1〜2秒 か 40〜70秒 かで、カルテ照会をどうするかの判断が
正反対になります。**測るまでカルテ照会を1通目から外す判断をしてはいけない**という
`program.md` の注意書きに同意します。

---

## 6. こちらから足すことは、§3 の3点だけです

コードについての修正依頼はありません。

`looksStaleConversation` を実測2文に絞っていただいた件、
**会話変数のエラー文で誤判定する恐れ**という理由までコメントに残っていました。
次に読む人が同じ心配をしなくて済みます。

---

## 使ったもの

- `patch -p1` → `node --check` → `node worker_retry_test.mjs`（作業側の58件）
- `tools/dify-test/worker_gate_test.mjs`（`p.at` の穴の再現。偽物のKV・HTTP）
- `tools/dify-test/worker_live_test.mjs`（**本物の Dify API**。KVだけ偽物）
- 実機はテスト用の利用者IDのみ。**本番のLINEには触れていません。**
- APIキー・合言葉・URLの値は、このファイルには書いていません。
