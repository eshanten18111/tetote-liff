# 同じ入口の文で1往復（`task_next.md` 第8版）

実施：2026-09-15 19:39（1往復のみ）
利用者ID：`t-v8-193949` ／ 会話ID：`7f579cd1-…`

---

## 0. 送った文が一致していることの確認

測定の全部がここに懸かっているので、**投げる前に検算しました。**

```
turn3.py : '呼び名：そうた／年代：30代／性別：男性／都道府県：徳島県／一番の悩み：仕事'
第8版     : '呼び名：そうた／年代：30代／性別：男性／都道府県：徳島県／一番の悩み：仕事'
一致      : True
文字数    : 38 / 38
```

**1文字も変えていません。**

---

## 1. 取れた値（そのまま）

```
所要         : 18.9 秒
入力トークン : 29,781
出力トークン : 663
入力の単価   : 0.75
出力の単価   : 3.75
```

---

## 2. §2 の表に当てはめた結果

比較する相手は 09-14 18:58 の1通目3回：**`29,752 / 29,757 / 29,762`（幅10）**。

```
今回 29,781

  29,752 との差  +29
  29,757 との差  +24   （中央値との差）
  29,762 との差  +19   （3回の最大との差）
```

| 表の行 | 当てはまるか |
|---|---|
| 29,820前後（＋60〜80）→ 公開が反映されている | **当てはまりません**（+19〜+29 で、+60 に届きません） |
| 29,752〜29,762（前と同じ幅の中）→ 反映されていない | **当てはまりません**（幅10の外です） |
| **どちらでもない中間の値 → 断定しない。値を報告する** | **これです** |

### したがって、判定は「断定しない」です

**公開されたとも、されていないとも言いません。**
第8版 §2 が「幅10の外だが+60未満、など」として、まさにこの場合を
**あらかじめ中間として扱うと決めていた**ためです。

```
確かめたこと   … 同じ文で投げて 29,781 だった。09-14 の3回は 29,752〜29,762 だった
                 差は +19〜+29 で、幅10の外にあり、+60 には届かない
確かめていないこと … その差が何から来ているか
```

---

## 3. 参考（判定には使っていません）

第8版 §2 の目安「48字 → +60〜80」は、**1字あたり約1.5トークン**という見積りから来ています。
今回の差を同じ式に戻すと、こうなります。

```
+24（中央値との差） ÷ 48字 ＝ 1字あたり約0.5トークン
```

> **これは公開の有無を決める材料ではありません。** §2 が「それ以外の材料で
> 公開の有無を語らないこと」と定めているので、ここから先は書きません。
> **ただ、目安の式そのものが今回の差と合っていない**、という数字だけ置いておきます。
> 足した48字が何のトークンになるかは、文字数だけでは決まりません。

**この節を読んで「では公開されている」と結論しないでください。** 私はしていません。

---

## 4. 所要について（第8版 §3）

```
今回            18.9 秒
第7版（19:26）  94.5 秒
09-14 18:58     58.4 / 82.4 / 23.4 秒
```

**第7版の94.5秒に対して、今回は18.9秒でした。**
**原因は名指ししません。** 内訳（`node_finished`）を取っていないので、
GASなのかLLMなのか、この2回の数字からは分かりません。

---

## 5. 実行の条件

```
往復数     … 1往復だけ（§4）。2往復目は投げていません
送った文   … 1文字も変えていません（§0 で検算済み）
設定       … Dify の下書きは1つも変えていません
利用者ID   … t-v8-193949（t-v8- で始まる）
本番のLINE … 触れていません
返事の中身 … 判定していません（§4）
鍵         … この環境はプロキシが鍵を入れるため DIFY_API_KEY を渡していません。
              指示の script は key が無ければヘッダを足さない作りなので、そのまま動きます。
              鍵の値は読んでいません
```

**指示の script は、そのまま使いました**（第7版と違い、今回は最初からストリーミングでした）。

---

## 6. `loop_log.md` への追記行（第8版 §5）

```markdown
| 09-15 19:39 | （確認のみ）同じ入口の文で公開の反映を確かめる | 1 | — | — | — | なし | — | 入力トークン29,781（09-14の3回は29,752〜29,762、幅10）→ 差+19〜+29。**§2 の3行目＝中間のため断定しない**（+60〜80には届かず、前の幅の中でもない）。出力663／所要18.9秒（第7版は94.5秒）。送った文は turn3.py の1通目と一致を検算済み。利用者ID t-v8-193949 |
```

---

## 7. 台帳に増えた行（第8版 §6）

**相談記録に1行**（会話IDは `t-v8-` で始まる）。**消すのは人です。**

---

## 8. 次にやること

**この測定では決まりませんでした。** 第8版 §2 が定めた3つの枝のうち、
「断定しない」に落ちたためです。**同じ手をもう一度打っても、同じところに落ちます。**

決めたいなら、**入力トークン以外の観測が要ります。** ただし、どれが取れるかは
作業側のほうが分かるはずです（公開済みの版を読む口が無い、というのが第7版 §0 の前提でした）。
**こちらから「こうすれば分かる」とは書きません。** 指示が来れば流します。
