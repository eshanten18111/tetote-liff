# `/ledger` を通したあとの1通目（`task_next.md` 第15版）

実施：2026-09-16 14:26〜14:29（JST）
利用者ID：`t-v13-mark-052601`（目印）／`t-v15a-052645-1` 〜 `-6`

---

## 0. 手順1 — 目印は **2026-09-16c**

§8 の「今の目印」と一致しました。手順2へ進みました。

---

## 1. 【1通目・6回】

| 回 | t_last−t0 | t_end−t0 | カルテ照会(秒/status) | 初回受付(秒/status) |
|---|---|---|---|---|
| 1 | 12.9 | 14.2 | 2.827 / 200 | 0.062 / 200 |
| 2 | 16.2 | 17.5 | 2.340 / 200 | 0.049 / 200 |
| 3 | 23.0 | 24.3 | 5.262 / 200 | 0.102 / 200 |
| 4 | 16.0 | 17.1 | 4.129 / 200 | 0.049 / 200 |
| 5 | 15.0 | 15.8 | 4.132 / 200 | 0.033 / 200 |
| 6 | 14.7 | 15.8 | 4.137 / 200 | 0.020 / 200 |
| **p50** | **15.5** | 16.5 | 4.130 | 0.049 |
| **max** | **23.0** | 24.3 | 5.262 | 0.102 |

```
24秒を超えた回 … 0/6（over24pct = 0）
0字の返事      … なし
status_code 0  … なし（12ノードすべて 200）
```

## 2. 【2〜5通目】（実行1の会話を4通伸ばした）

| 通目 | t_last−t0 | 初回受付(秒/status) | 返事の長さ |
|---|---|---|---|
| 2 | 16.9 | 0.045 / 200 | 120字 |
| 3 | 14.3 | 0.055 / 200 | 170字 |
| 4 | 18.2 | 0.133 / 200 | 151字 |
| 5 | 17.5 | 0.067 / 200 | 159字 |

**2〜5通目も24秒を超えた回はありません。**

---

## 3. 第10版（改修前）との比較（§2 で書いてよいとされた範囲）

| | 第10版 09-16 未明 | **今回 09-16 14:26** |
|---|---|---|
| 1通目 t_last p50 | **58.3秒** | **15.5秒** |
| 1通目 t_last max | **166.7秒** | **23.0秒** |
| 1通目 24秒超 | **100%（6/6）** | **0%（0/6）** |
| 初回受付の elapsed | 8.0〜19.5秒（送り直しが走った回あり） | **0.020〜0.133秒** |
| 0字の返事 | — | なし |

```
1通目は p50 58.3秒 → 15.5秒、max 166.7秒 → 23.0秒、24秒超は 100% → 0% でした。
```

**原因の名指しと次の改修の提案は書きません**（§2）。

---

## 4. §3 の「見てほしい異常」

### ① `via:worker` — **6回すべてで見えました**

```
実行1〜6 すべて: {"ok":true,"queued":1,"via":"worker"}
```

**§3 のとおり、`/ledger` を通っている証拠です。**

### ② カルテ照会の `pending:true` — **6回中4回**

```
実行1: {"ok":true,"found":false,"visits":0}      ← GAS の答えが返っている
実行2: {"ok":true,"found":false,"visits":0}      ← 同上
実行3: {"found":false,"pending":true}            ← pending
実行4: {"found":false,"pending":true}            ← pending
実行5: {"found":false,"pending":true}            ← pending
実行6: {"found":false,"pending":true}            ← pending
```

**その4回のカルテ照会の所要は 5.262 / 4.129 / 4.132 / 4.137 秒**、
`pending` でなかった2回は **2.827 / 2.340 秒**でした。

```
確かめたこと   … pending だった4回はいずれも 4.1秒以上、そうでない2回は 2.9秒未満
                 §3 に「Worker が4秒で見切って pending を返す設計」と書かれていること
確かめていないこと … Worker の中で何が起きているか（KV も Worker のログも見えません）
```

**今回の利用者はどれもカルテを持たないので、`found:false` という結果自体は
`pending` の有無にかかわらず同じでした。** カルテ持ちの方でどうなるかは、
**この測定では分かりません。**

### ③ `status_code 0` — なし

### ④ 0字の返事 — なし

前回（案2）は10通目が0字でした。**今回は10通すべてに本文があります。**

---

## 5. 送った文について（先に申し上げた食い違い）

**第15版 §1 手順2 には食い違いがありました。**

```
prose … 「第8版・第9版・第10版と同じ入口の文（1文字も変えないこと）」
block … 「呼び名：めぐみ／年代：40代／性別：女性／都道府県：秋田県／一番の悩み：その他」
```

リポジトリで確認したところ、**第8・9・10版が実際に送っていたのは そうた** でした。

```
tools/dify-test/turn3.py:12          "呼び名：そうた／年代：30代／性別：男性／都道府県：徳島県／一番の悩み：仕事"
tools/dify-test/v9_node_clock.py:2   同上
tools/dify-test/v10a_after_answer.py:2  同上
```

**§1 手順2 が「比べたいので、形を変えないこと」と書いているので、
比較が成立するほう（そうた）で流しました。**

```
確かめたこと   … 第8・9・10版が そうた を送っていたこと（3本のスクリプトで確認）
確かめていないこと … めぐみ で測った場合に数字が変わるか
```

**めぐみ での測り直しが必要なら、おっしゃってください。** 6回で5分ほどです。
（`めぐみ` は手順1の目印確認で毎回送っている文なので、そちらは今回も1通送っています）

---

## 6. 実行の条件

```
手順1 … 1通（t-v13-mark-052601）。ストリーミング
手順2 … 6回（t-v15a-052645-1 〜 -6）。ストリーミング
手順3 … 実行1の会話を4通伸ばした（行は増えません）
設定   … Dify の下書きは1つも変えていません
GAS_URL … 触っていません（§4）
Worker … 触っていません
本番のLINE … 触れていません
鍵     … プロキシが入れるため DIFY_API_KEY を渡していません。値は読んでいません
投げ方 … すべてストリーミング（§4 の「blocking で投げない」に従いました）
回数   … 1通目6回＋伸ばす会話1本で止めました（§4 の上限どおり）
```

---

## 7. 台帳（§6）

```
相談記録に 7行（目印確認1行＋1通目6行）
　　手順3 は実行1の会話を伸ばしたので、行は増えていません
```

**消すのは人です。** Slack への通知は出ていないはずです（この台本は着地しません）。

---

## 8. `loop_log.md` への追記行（§5）

```markdown
| 09-16 14:26 | （測定）/ledger 後の1通目 | 6+4 | **15.5** | **23.0** | **0** | なし | — | **1通目 p50 15.5／max 23.0／24秒超 0%**（第10版は 58.3／166.7／100%）。カルテ照会 p50 4.130・max 5.262（全200）、初回受付 p50 0.049・max 0.133（全200）。**via:worker が6回すべてで見えた**（{"ok":true,"queued":1,"via":"worker"}）。**pending:true が6回中4回**（その4回はカルテ照会 4.13〜5.26秒、pendingでない2回は 2.34／2.83秒）。**0字なし・status_code 0 なし**。2〜5通目も 14.3〜18.2秒で24秒超なし。**送った文は そうた**（§1の引用は めぐみ だが、第8・9・10版の実測は そうた。比較のためそちらを採用）。利用者ID t-v15a-052645。台帳7行 |
```

---

## 9. 次にやること

**こちらからの提案はありません。** 数字は上に出しました。

**測れます（指示があれば）**

```
・めぐみ の文での測り直し（§5 の食い違いを埋める場合）
・カルテ持ちの方での pending の挙動（**ただし、この環境ではカルテを作れません。
  refreshKarte が15分ごとに走るので、前の会話を置いてから15分あける必要があります**）
・第11版（6通目以降の「回答の後ろ」の77秒）— 保留のままです
```

**測れません**

```
・支援経過シート／相談記録シートの中身
・Worker の KV・ログ（pending の中で何が起きているか）
```
