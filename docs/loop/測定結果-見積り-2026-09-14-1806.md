# 測定結果（2026-09-14 18:06 JST）— 見積りは出せませんでした

実行：`nodetime3.py`（task_next.md 18時台版 §1-1 の a〜c をそのまま実装）
条件：1通目のみ／カルテなし／**設定は1つも変えていない**／クライアント側タイムアウト360秒

---

## 0. 判定：§2 の表の5つめの枝

> | 実測 < ノード合計 の回がある | **直列の前提が崩れている** | 見積りを出さず、その事実だけを報告（1-3 の注意） |

```
実行7  実測 102.5秒 ／ ノード合計 108.3秒 ／ 差 -5.8秒
```

**ノードの所要時間の合計が、会話全体の実測を5.8秒上回りました。**
どこかのノードが並列に走っています。

指示どおり、`B見積り` `A見積り` `A+B見積り` は**計算していません。**
階級の集計も、式と同じ前提に立つため出していません。

---

## 1. 12回の生データ

| 回 | 実測 | ノード合計 | 差 | カルテ照会 | 初回受付 |
|---|---|---|---|---|---|
| 1 | 54.2 | 51.4 | 2.8 | 2.63 | **34.56** |
| 2 | 22.0 | 19.2 | 2.8 | 3.15 | 1.04 |
| 3 | 14.6 | 12.7 | 1.9 | 1.83 | 1.13 |
| 4 | 19.9 | 17.5 | 2.4 | 1.89 | 1.22 |
| 5 | 73.6 | 70.5 | 3.1 | **58.29** | 1.16 |
| 6 | 20.4 | 17.8 | 2.7 | 3.22 | 1.37 |
| **7** | **102.5** | **108.3** | **-5.8** | 1.71 | **72.76** |
| 8 | 36.7 | 33.0 | 3.7 | **18.64** | 0.99 |
| 9 | 14.9 | 13.2 | 1.7 | 2.11 | 0.86 |
| 10 | 24.6 | 22.7 | 1.9 | 6.81 | 1.05 |
| 11 | 56.7 | 54.3 | 2.3 | 1.78 | **42.24** |
| 12 | 52.6 | 50.4 | 2.2 | 2.32 | **36.73** |

```
n=12  p50=25.4  min=14.6  max=102.5  over24=7  over24pct=58
```

---

## 2. 報告すべき事実は2つあります

### 事実1：直列の前提が崩れている（実行7）

差が **−5.8秒**。1回だけですが、**式の前提が成り立たない証拠としては十分**です。

`task_next.md §1-3` にこうあります。

> 見積りを出すより、前提が崩れたほうが重要である。

**どのノードが並列に走っているかは、配線を見れば分かるはずです。**
こちらからは実行順しか見えません。

参考までに、実行7のノード完了順に異常はなく、すべて `succeeded` でした。

### 事実2：設定値の上限を超えている

| ノード | 今回の max | `§7G` の「最悪」 | 超過 |
|---|---|---|---|
| カルテ照会 | **58.29秒** | 32秒 | **+26秒** |
| 初回受付 | **72.76秒** | 32秒 | **+41秒** |

`connect 5 ＋ read 10 ＝ 15秒`、再試行1回・間隔2000ms で `15 + 2 + 15 = 32秒`。
**58.29秒と72.76秒は、この式では説明できません。**

考えられるのは、

- 再試行が**2回以上**走っている（設定と実際が違う）
- **read タイムアウトが効いていない**（接続は生きているがデータが来ない状態では
  `max_read_timeout` が発火しない、など）
- そもそも別の待ちが入っている

**どれであるかは、こちらからは判別できません。** 確認をお願いします。

---

## 3. この時間帯は前回よりはるかに悪い

| | 17:38（前回） | 18:06（今回） |
|---|---|---|
| over24pct | 33% | **58%** |
| p50 | 21.3 | 25.4 |
| max | 60.6 | **102.5** |
| カルテ照会 max | 17.25 | **58.29** |
| 初回受付 max | 27.17 | **72.76** |

**28分しか離れていません。** GAS側の状態が短時間で大きく変わることを示しています。

これは `§7F` の「時刻を変えてもう1回」（4枝め）の答えにもなっています。
**時刻依存は実在します。ただし Gemini ではなく GAS 側です。**

---

## 4. 打ち手について（方向は変わりません。見積りだけが出せません）

**カルテ照会と初回受付が跳ねの出どころ**という `§7G` の結論は、今回も一貫しています。

```
over24 した7回のうち
  初回受付が18秒以上 … 4回（34.56 / 72.76 / 42.24 / 36.73）
  カルテ照会が18秒以上 … 2回（58.29 / 18.64）
  どちらも18秒未満    … 1回（実行10：カルテ照会 6.81・初回受付 1.05・実測24.6）
```

打ち手A（初回受付を回答より後ろへ）と
打ち手B（カルテ照会を 3/5/3・再試行なし）の**狙いは正しいままです。**

ただし **「最悪32秒」という前提が崩れた**ので、
**Bの効き（17.25秒 → 最悪8秒）という見積りも、そのままでは使えません。**
`read 5秒` に詰めても、いま `read 10秒` が効いていないのであれば、同じことが起きます。

**事実2の確認が済むまで、Bの効きは見積もれません。**

---

## 5. `loop_log.md` への追記行

```markdown
| 09-14 18:06 | （測定のみ）階級と打ち手見積り | 12 | 25.4 | 102.5 | 58 | なし | — | **§2の5枝め：実行7で 実測102.5 < ノード合計108.3（並列の証拠）。見積りは出さず**。カルテ照会 max 58.29／初回受付 max 72.76 で、いずれも §7G の「最悪32秒」を超過。設定値だけでは説明がつかない |
```

### 「次にやること」の書き換え案

```markdown
## 次にやること

**測り直す前に、2つの事実を作業側に確認してもらう。**

1. **実測 < ノード合計 が起きる**（実行7：102.5 < 108.3）。
   どのノードが並列に走っているか。配線を見れば分かるはず
2. **カルテ照会 58.29秒・初回受付 72.76秒**が出た。
   §7G の「最悪32秒」を超えている。再試行が2回以上走っていないか、
   read タイムアウトが実際に効いているかを確認

**1が解けるまで、打ち手A・Bの見積りは計算できない**（直列を前提にした式が使えないため）。
**2が解けるまで、打ち手Bの効きは見積もれない**（`read 5秒` に詰めても、
いま `read 10秒` が効いていないなら同じことが起きる）。

打ち手A・Bの**方向は変わらない**。カルテ照会と初回受付が出どころである点は
今回の12回でも一貫している（over24 した7回のうち6回で、どちらかが18秒以上）。
```

---

## 6. 台帳に増えた行数

**相談記録に12行**（呼び名「そうた」・30代・男性・徳島県・仕事）。**消すのは人。**

---

## 7. 測定方法

スクリプト：`tools/dify-test/nodetime3.py`
（`eshanten18111/tetote-liff` の `claude/dify-testing-continued-j1xrb4`）

`§1-3` の注意も実装しています。

```python
broken = [r for r in rows if r["wall"] < r["total"]]
if broken:
    print("!! 実測 < ノード合計 の回がある（並列に走っている証拠）。式は使わない。")
    sys.exit(0)
```

**1回でも並列の証拠が出たら、見積りを出さずに終了します。**
