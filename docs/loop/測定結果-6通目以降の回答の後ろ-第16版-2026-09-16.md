# 6通目以降の「回答の後ろ」に何があるのか（`task_next.md` 第16版）

実施：**2026-09-16 14:38:39 〜 14:41:53（JST）**
利用者ID：`t-v13-mark-053801`（目印）／`t-v16-053839` ／ 会話ID `8ea7b4a2-…`

---

## 0. 結論

**差が大きい回は、1回も出ませんでした。差は 0.2〜1.6秒（p50 1.2秒）です。**

**仮説（パラメータ抽出3）は、当たっても外れてもいません。**
そのノードは6回とも **1.157〜1.555秒**で、77秒に近づく気配はありませんでした。
ただし**跳ねが1回も出ていない**ので、**跳ねたときにそのノードが何秒だったかは、まだ誰も見ていません。**

§1 の「差が大きい回が1回も出なかったら、それも結果である。そう書いて止めること」に従い、
**回数は増やしていません。**

---

## 1. 手順1 — 目印は **2026-09-16c**

§7 の「今の目印」と一致しました。手順2へ進みました。

---

## 2. 表

| 通目 | t_first−t0 | t_last−t0 | t_end−t0 | **差** |
|---|---|---|---|---|
| 6 | 15.7 | 15.7 | 17.0 | 1.3 |
| 7 | 21.8 | 21.8 | 23.1 | 1.2 |
| 8 | 12.4 | 12.4 | 13.6 | 1.2 |
| 9 | 19.9 | 19.9 | 20.0 | **0.2** |
| 10 | 18.7 | 18.7 | 20.3 | **1.6** |
| 11 | 16.3 | 16.3 | 17.3 | 1.0 |
| **p50** | | | | **1.2** |
| **max** | | | | **1.6** |

**6通とも `t_first` と `t_last` が同じ値でした**（0.1秒の桁まで）。
返事が1回のかたまりで届いています。第10版では `first_token` が実測の1.4秒前でしたが、
**今回はその差がありません。** 数字として置いておきます。

---

## 3. 差の中で動いたノード（第10版Bで欠けていたところ）

**6通とも、まったく同じ5ノードでした。全部 `succeeded` です。**

```
返信整形（サマリー除去）          0.099〜0.470秒
回答                              0.0001〜0.0002秒
IF/ELSE 6（引き継ぎサマリー有無）  0.0001秒
パラメータ抽出3（有人対応同意）    1.157〜1.555秒
IF/ELSE 4（有人対応同意判定）      0.0001〜0.0003秒
```

**パラメータ抽出3 の6回ぶんの実測**

```
1.157 / 1.239 / 1.269 / 1.271 / 1.500 / 1.555 秒   （幅 0.40秒）
```

`t_last` から何秒後に受け取ったかも採りました。

```
6通目  t_last+1.2秒   7通目  t_last+1.0秒   8通目  t_last+1.0秒
9通目  t_last+0.1秒  10通目  t_last+1.5秒  11通目  t_last+0.8秒
```

**9通目だけ差が0.2秒**ですが、そのときもパラメータ抽出3 は 1.238秒動いています。
**つまりこのノードは `t_last` より前から動き始めていて、差に出るのは「はみ出したぶん」だけです。**
（第10版で作業側が指摘した「送り直しの経路は LLM→回答 と並列に走る」のと同じ形です）

---

## 4. 77.4秒について、いまどこまで分かっているか

**この問いは、これで2回測って2回とも再現していません。**

| いつ | 局面 | 状態 | 差 |
|---|---|---|---|
| 09-15 14:47（第10版B） | 6通目以降 3回 | 改修前 | 1.3 ／ **77.4** ／ **30.1** |
| 09-16 02:19（第11版相当） | 6通目以降 6回 | **GAS_URL 事故の最中** | 1.0〜1.5 |
| **09-16 14:38（今回）** | 6通目以降 6回 | **改修後（v7＋/ledger）** | **0.2〜1.6** |

```
確かめたこと   … 改修後の昼に6回測って、差は 0.2〜1.6秒だったこと
                 その中身は毎回同じ5ノードで、パラメータ抽出3 は 1.157〜1.555秒だったこと
確かめていないこと … 77.4秒の中で何が動いていたか
                 跳ねが起きる条件（時間帯・状態・そのほか）
```

**「もう起きない」とは書きません。** 12回の観測で出なかった、というだけです。
**「パラメータ抽出3 ではなかった」とも書きません。** 跳ねたときの値を誰も見ていないためです。

---

## 5. 実行の条件

```
回数   … 下ごしらえ5通＋測定6通＝11通（§3 の上限どおり。1会話を使い回しました）
送った文 … 下ごしらえは第10版Bと同じ5通。測定の6通は09-16 02:19 と同じ6通
設定   … Dify の下書きは1つも変えていません
GAS_URL … 触っていません（§3）
Worker … 触っていません
本番のLINE … 触れていません
鍵     … プロキシが入れるため DIFY_API_KEY を渡していません。値は読んでいません
投げ方 … すべてストリーミング（§3 の「blocking で投げない」に従いました）
```

---

## 6. 台帳（§5）

```
相談記録に 2行（目印確認 t-v13-mark-053801 で1行、t-v16-053839 で1行）
　　11通は同じ会話で伸ばしたので、行は増えていません
```

**消すのは人です。** Slack への通知は出ていないはずです（この台本は着地しません）。

---

## 7. `loop_log.md` への追記行（§4）

```markdown
| 09-16 14:38 | （測定）6通目以降の回答の後ろ | 6 | 17.5 | 21.8 | 0 | なし | — | **差 p50 1.2／max 1.6（0.2〜1.6秒）。差が大きい回は出なかった。** 中身は6通とも同じ5ノード・全部 succeeded。**パラメータ抽出3 は 1.157〜1.555秒**（幅0.40）。**仮説は当たっても外れてもいない**（跳ねが出ないので、跳ねたときの値を誰も見ていない）。9通目だけ差0.2秒だが、そのときもパラメータ抽出3 は1.238秒＝このノードは t_last より前から動いており、差に出るのははみ出したぶんだけ。6通とも t_first = t_last（返事が1かたまりで届く）。**77.4秒はこれで2回測って2回とも再現せず**（02:19 深夜／今回 14:38 昼・改修後）。利用者ID t-v16-053839。台帳2行 |
```

---

## 8. 次にやること

**こちらからの提案はありません**（§2）。

**残っているのは「77.4秒が起きる条件が分からない」ことだけです。**
再現していないので、**次に何を測れば近づくのかも、こちらからは言えません。**

```
測れます（指示があれば）
  ・同じ形で別の時間帯に測る（ただし何回やれば出るかは分かりません）
  ・跳ねを捕まえる仕掛けを置いて待つ（課金と台帳の行が増え続けます）

測れません
  ・Dify の実行ログ（401。program.md §4）
  ・Worker の KV・ログ
```

**「相談者には見えないから良い」とは書きません。** §0 のとおり、
Dify 側では実行が続き課金が乗る、という作業側の整理をそのまま置いておきます。
