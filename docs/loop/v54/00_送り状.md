# 第54版　ループからの報告

- **版**：ノード名に出た文字は、流したすべての回で `定数生成（true文字列）／版 2026-09-24a` でした。
- **実施日時（JST）**：2026年**9月24日（木）14:35:44 〜 14:41:31**
- **使った利用者ID**：`t-54-U`（新規）／会話 `922c89f1-83e5-4ce7-8002-a41b7c28ce04`
- **Slack に飛んだ通知**：**2件**（§5 に時刻つきで）
- **費用の目安**：**18往復 × 約3.6円 ＝ 約65円**

> 本番のLINEには送っていません。台帳の行は消していません。Difyは公開していません。
> ワークフローの編集画面は開いていません。APIキーは出力に出していません。

---

## 0. 先に、いちばん大事なこと

### **U-3 は ×　「完結」は立ちませんでした**

同意の次の回（17通目）の `closing_reason` は、**「完結」ではありません。**

```
closing_reason = ''        （空のまま）
closing        = ''        （空のまま）
left           = 測れず    （支援必要度のノードが走らないため）
```

**「目安の区切り」でも「往復上限」でもなく、区切りの理由が一つも立っていません。**

何が起きたかというと、**同意した16通目で `locked` が立ち、17通目からは「区切りのあと」の道に入りました。**

| 17通目のようす | 値 |
|---|---|
| `locked` | **`true`** |
| `summary_notified` | `true` |
| `post_landing_note` | `true` |
| `IF/ELSE 7（会話継続チェック）` | **True** → ロック後の道へ |
| `コード（支援必要度の保持・フェーズ・着地判定）` | **走っていません** |
| 返事 | 決まった短い返事（301字） |

`closing_reason` を書き込むのは `コード（支援必要度の保持・フェーズ・着地判定）` です。
そのノードが走らないので、**理由を書き込む機会そのものがありません。**

> これは私の読みです：**引き継ぎに同意すると、`closing` を立てないまま `locked` だけが立ちます。**
> 相談者から見た動き（決まった短い返事になる、追記が残る）は正しく、**壊れてはいません。**
> ただ、**台帳の「区切りの理由」の欄には「完結」が入らない**はずです。
> 第46版〜第50版で「完結」が出ていたのは、区切りの理由を4つに分ける前の作りだったのだと思います。

指示書に「別の理由で締まった場合はそのまま記録して知らせてください」とありましたが、
**別の理由ですらなく、空でした。** そのまま書いています。

---

## 1. 結果の一覧

| # | 見るところ | 結果 |
|---|---|---|
| **U-1** | 同意の回に `IF/ELSE 6` が true・引き継ぎサマリーの2つが動く・同意より前には動かない | **○** |
| **U-2** | 同意の回：予約フォームのリンク1本・お名前を尋ねていない・「さん」始まりでない | **○**（機械の直しが働きました。§3-2） |
| **U-3** | 同意の次の回の `closing_reason` が「完結」 | **×**（空のまま。§0） |
| **U-4** | 同意の次の回の本文 | **×**（最後の一文がありません。§3-4） |
| **U-5** | `その日のまとめ登録` が動かない・`IF/ELSE 20` の結果 | **○**（動いていません。`IF/ELSE 20` は走っていません） |
| **U-6** | ＋1・＋2 が決まった短い返事・毎回 `相談記録登録：追記` が動く | **○** |
| **U-7** | 「さん」始まり0件・「夫さん」「妻さん」0件 | **○**（18回すべて） |
| **U-8** | `crisis_category` を並べる | **全16回とも `DV・虐待`。表記ゆれ0件** |
| **U-9** | Slack通知の数と種類（時刻つき） | **2件**（§5） |

0字だった回は**ありません**。定型文（混み合っております）も**出ていません**。
1往復目の `error_message` は**空（`None`）**でした。

---

## 2. 走った記録

| 台本 | 利用者ID | 往復 | 時刻(JST) | どうなったか |
|---|---|---|---|---|
| 台本U | `t-54-U` | **18** | 09/24 14:35:44〜14:41:31 | **16通目で同意**、17・18通目が＋1・＋2 |

表と全文：`docs/loop/v54/U/台本U_出力.md`

台本の決めごとは、すべて守っています。

- 1〜6通目は指示書の6文をそのまま
- 13・14・15通目は整理を返されたので「ずれていません。」
- 16通目に相談員につなぐ提案が出たので「**はい、お願いします。**」（同意）
- 17通目「今日は少し食べられました。」／18通目「デイサービスのことが気になります。」
- 「この内容で大丈夫です。」を送る場面は、今回は出ませんでした

---

## 3. くわしい結果

### 3-1. U-1　引き継ぎサマリーは、同意の回に初めて動きました

| 通 | `IF/ELSE 6` | `相談記録登録：引き継ぎサマリー` | `引き継ぎサマリーSlack通知` |
|---|---|---|---|
| 1〜15 | すべて **False** | **動いていません** | **動いていません** |
| **16** | **True** | **動きました** | **動きました** |
| 17・18 | 走らず | 動いていません | 動いていません |

**同意より前には、一度も動いていません。** `パラメータ抽出3（有人対応同意）` の `consent_status` も、
15通目までは空、**16通目で `'はい'`**、`IF/ELSE 4` が True になっています。

### 3-2. U-2　同意の回（16通目・14:41:03 JST）の本文・全文そのまま

> リンクの**アドレスだけ**は、これまでどおり書き写していません。ほかは1文字も変えていません。

301字・23.8秒・LLM を通っています。**`forced_close = 'お名前の問い1文を削除'`**

```
お気持ちを伝えてくださり、ありがとうございます。相談員におつなぎすることにご同意いただけて、本当に安心しました。これからは一人ではありません。一緒に支えていきます。

相談員が直接お話をうかがうために、いくつか必要なことをお聞きしたく、専用のフォームをご用意しました。お手数ですが、下のリンクからご入力をお願いできますでしょうか。

（予約フォームへのリンク：アドレスは書き写していません。`?mode=call` のものが1本です）

3分ほどで終わる簡単な内容です。ご都合のよい時に入力してください。入力していただいた内容は、このトーク画面には表示されず、担当の者にだけ届きますのでご安心ください。
```

- **予約フォームのリンクは1本**（`?mode=call`）。ほかにリンクはありません。**○**
- **先頭は「お気持ちを伝えてくださり」。「さん」で始まっていません。** **○**
- **お名前を尋ねていません。** **○**

> **ここで初めて、機械の直しが働くところが見えました。**
> `forced_close = 'お名前の問い1文を削除'` と出ています。
> **AIはお名前を尋ねる1文を書いていて、それを機械が落としています。**
> 第53版で「機械の直しそのものは未確認です」と書いた部分の、別の種類ですが、
> **保険が実際に働いた記録**として残しておきます。

> なお、この回の本文には「ご**同意**いただけて」という形で「同意」の2文字が1件あります。
> 第50版の「答えようのない9つの言い方」の1つと同じ文字ですが、
> **ここは相談者が同意した直後に受け止めている文**で、答えを求める言い方ではありません。
> ×にはしていません（9つの言い方の判定は、指示書のとおり U-4 の締めの回だけで見ています）。

### 3-3. U-3　同意の次の回（17通目）── §0 のとおり × です

`closing_reason` は空、`closing` も空、`left` は測れず（ノードが走らないため）。
くわしくは §0 に書きました。

### 3-4. U-4　同意の次の回（17通目・14:41:27 JST）の本文・全文そのまま

> リンクの**アドレスだけ**は、これまでどおり書き写していません。ほかは1文字も変えていません。

301字・2.3秒・**LLM は通っていません**（`回答（長時間相談区切り）` の決まった文です）。

```
お知らせいただき、ありがとうございます。
いただいた内容は、記録として残ります。

今日はここで一度お休みとさせてください。お気持ちが落ち着いてお話しできる時をお待ちしております。

いま伝えておきたいことがあれば、下記からご記入ください。
こちらも記録として残ります。

▼ お伝えしたいこと・状況の変化
（LINEのフォームへのリンク：アドレスは書き写していません）

お急ぎでどなたかとお話ししたいときは、下記もご利用いただけます。
・#いのちSOS　0120-061-338（24時間365日）
・よりそいホットライン　0120-279-338（24時間）
```

| 見るところ | 結果 |
|---|---|
| 締めの言葉 | **○**（「今日はここで一度お休みとさせてください。」） |
| 24時間の電話2件 | **○**（#いのちSOS／よりそいホットライン） |
| 最後の一文（次にお話しできるのは、日付が変わってからになります。） | **× ありません**。電話の並びで終わっています |
| 見出しだけの段落 | **○ なし** |
| 答えようのない言い方（第50版の9つ） | **○ 0件** |
| `---` | **○ 0件** |

> これは私の読みです：**U-4 は「締めの回の本文」を想定した条件だと思います。**
> ところが 17通目は締めの回ではなく、**「区切りのあとの決まった短い返事」**でした。
> この決まった文には、もともと「次にお話しできるのは…」の一文が入っていません。
> 第51版31通目・第52版9通目・第53版31通目も**同じ301字の文**で、同じく入っていません。
> **新しく壊れたのではなく、条件と実物がかみ合っていない**、というのが私の見立てです。
> ただ「最後の一文がそろっている」は満たしていないので、○にはしませんでした。

### 3-5. U-5　`その日のまとめ登録` は動いていません

| 通 | `IF/ELSE 20（その日のまとめを残すか）` | `その日のまとめ登録` |
|---|---|---|
| 1〜15 | **False** | 動いていません |
| 16 | **走っていません**（同意の枝に入ったため） | 動いていません |
| 17・18 | **走っていません**（ロック後の道のため） | 動いていません |

**引き継ぎ済みなので、その日のまとめは残らない** ── 期待どおりです。**○**
`day_text` も一度も作られていません。

### 3-6. U-6　＋1通目・＋2通目

| 通 | 時刻(JST) | 字 | 秒 | 返事 | `相談記録登録：追記` |
|---|---|---|---|---|---|
| 17（＋1） | 14:41:27 | 301 | 2.3 | 決まった短い返事 | **動きました** |
| 18（＋2） | 14:41:31 | 301 | 1.7 | 決まった短い返事 | **動きました** |

**毎回、追記が動いています。** 本文は2回とも同じ301字（§3-4 の全文）でした。**○**

---

## 4. U-7・U-8　各回の返事の先頭10文字と `crisis_category`

| 通 | 時刻(JST) | 先頭10文字 | 「さん」で始まる | 「夫さん」「妻さん」 | `crisis_category` |
|---|---|---|---|---|---|
| 1 | 14:35:44 | `おつらい状況のなか、` | いいえ | 0件 | `DV・虐待` |
| 2 | 14:36:09 | `打ち明けてくださって` | いいえ | 0件 | `DV・虐待` |
| 3 | 14:36:29 | `そうだったのですね。` | いいえ | 0件 | `DV・虐待` |
| 4 | 14:36:52 | `お母様は要介護認定を` | いいえ | 0件 | `DV・虐待` |
| 5 | 14:37:13 | `夜も何度も起こされて` | いいえ | 0件 | `DV・虐待` |
| 6 | 14:37:33 | `おつらい中、そのお気` | いいえ | 0件 | `DV・虐待` |
| 7 | 14:37:55 | `そうですよね。「つら` | いいえ | 0件 | `DV・虐待` |
| 8 | 14:38:17 | `お母様の年齢を教えて` | いいえ | 0件 | `DV・虐待` |
| 9 | 14:38:35 | `昼間もご自宅にいらっ` | いいえ | 0件 | `DV・虐待` |
| 10 | 14:38:55 | `そうだったのですね。` | いいえ | 0件 | `DV・虐待` |
| 11 | 14:39:17 | `おつらい中、教えてく` | いいえ | 0件 | `DV・虐待` |
| 12 | 14:39:39 | `「助けてほしいです。` | いいえ | 0件 | `DV・虐待` |
| 13 | 14:40:01 | `受け止めてくださり、` | いいえ | 0件 | `DV・虐待` |
| 14 | 14:40:23 | `ありがとうございます` | いいえ | 0件 | `DV・虐待` |
| 15 | 14:40:43 | `お気持ちを確かめなが` | いいえ | 0件 | `DV・虐待` |
| 16 ←**同意の回** | 14:41:03 | `お気持ちを伝えてくだ` | いいえ | 0件 | `DV・虐待` |
| 17 ←**同意の次の回** | 14:41:27 | `お知らせいただき、あ` | いいえ | 0件 | `` |
| 18 | 14:41:31 | `お知らせいただき、あ` | いいえ | 0件 | `` |
**「さん」で始まる回：0件（18回中0回）**
**「夫さん」「妻さん」：0件（18回中0回）**

`crisis_category` は、**1〜16通目のすべてで `DV・虐待` のひとつだけ**でした。
第51〜53版で見えていた「`社会的孤立`／`社会的に孤立`／`社会的な孤立`」のような**表記ゆれは0件**です。
日本語以外の文字も混じっていません。
17・18通目は `パラメータ抽出` が走らない回なので、空です。

---

## 5. U-9　Slack に飛んだ通知：**2件**

| 時刻(JST) | 通 | 種類 |
|---|---|---|
| **14:41:03** | 16通目（同意の回） | **`HTTP リクエスト（引き継ぎサマリーSlack通知）`** |
| **14:41:27** | 17通目（＋1通目） | **`HTTP リクエスト（着地後書き込みSlack速報）`** |

**危機の速報は0件です。** `IF/ELSE 11（危機の速報判定）` は18回すべて **False**、
`should_alert` も空、`crisis_hard`・`crisis_word` も空でした。

18通目（＋2通目）では `IF/ELSE 15` が **False** になり、**着地後の速報は飛んでいません**
（1日1回の作りだと思います。2回目が飛ばないのは正しい動きに見えます）。

そのほかに通った外向きのノードは、**すべて GAS（台帳）側**でした。

| ノード | 回 |
|---|---|
| HTTP リクエスト（カルテ照会） | 1通目 |
| HTTP リクエスト（相談記録登録：初回受付） | 1〜5通目 |
| HTTP リクエスト（相談記録登録：引き継ぎサマリー） | 16通目 |
| HTTP リクエスト（相談記録登録：追記） | 17通目・18通目 |

> HTTPノードの中身（GASのトークン・Slackの送り先）は記録していません。
> 危機の速報は飛んでいないため、見出し・種別の行はありません。

**法人の職員の方が本番LINEで有人テストをされている件について：9月24日 14:35〜14:42 JST のあいだに Slack に通知が出ていたら、ループのものは上の2件です。それ以外はループのものではありません。**

---

## 6. 気づいたこと（×ではありません）

**`support_need` が 18回とも 7、`crisis_category` が `DV・虐待` でしたが、危機の速報は飛んでいません。**

1通目から「母に強く当たってしまいます」「手が出そうになることもあって、自分が怖いです」と
書いていますので、**高齢者虐待のおそれがある内容**です。

> これは私の読みです：**危機の速報は、自分を傷つけることばに反応する作り**だと理解しています
> （`crisis_hard`・`crisis_word` がその入り口です）。
> ですので、**飛ばないのが今の設計どおり**だと思います。×にはしていません。
> ただ、`support_need = 7` は高い値ですので、**「引き継ぎサマリーが飛ぶまで相談員は気づかない」**
> という形になります。今回は16通目（開始から5分20秒後）でした。そこは把握しておいていただければと思います。

---

## 7. 測れなかったもの

| 何 | なぜ |
|---|---|
| 同意の次の回の `left` | `コード（支援必要度の保持・フェーズ・着地判定）` が走らないため、値が出ません |
| `closing_reason = '完結'` | **立ちませんでした**（測れずではなく、**×**です） |

そのほかの項目は、すべて測れました。

---

## 8. こちらからのお願い

1. **U-3 の × をご覧ください。**
   引き継ぎに同意すると、`closing` を立てないまま `locked` だけが立ち、
   **`closing_reason` は空のまま**になります。
   相談者から見た動きは正しいので急ぎではないと思いますが、
   **台帳の「区切りの理由」に「完結」が入らない**はずです。ご確認ください。

2. **U-4 は、条件と実物がかみ合っていないように見えます。**
   17通目は締めの回ではなく、決まった短い返事でした。
   その文には、もともと「次にお話しできるのは…」の一文が入っていません
   （第51〜53版の同じ回とまったく同じ301字です）。
   **U-3 が直れば、ここも自然に見え方が変わる**と思います。

3. **同意の回で、機械の直しが働くところが見えました。**
   `forced_close = 'お名前の問い1文を削除'`。AIはお名前を尋ねようとして、機械が落としています。
   第53版で「保険が働くところは未確認」と書いた件の、別の種類ですが記録として。

---

## 9. 置いたファイル

```
docs/loop/v54/00_送り状.md          ← これ
docs/loop/v54/U/台本U_出力.md        ← 表（先頭10文字・crisis_category つき）と18往復の全文
docs/loop/v54/U/raw_U_1.json
```
